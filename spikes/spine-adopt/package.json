{
  "name": "@spike/spine-adopt",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Disposable adopt-round spike (mmnto-ai/totem-strategy#1138). Not a workspace package: pnpm-workspace.yaml globs `packages/*` only, so `pnpm -r` never sees this.",
  "scripts": {
    "census": "node --experimental-strip-types src/census.mts",
    "verify-records": "node --experimental-strip-types src/verify-records.mts",
    "facts": "node --experimental-strip-types src/facts.mts",
    "shipped-verdicts": "node --experimental-strip-types src/shipped-verdicts.mts",
    "all": "npm run census && npm run verify-records && npm run facts && npm run shipped-verdicts",
    "lower": "node --experimental-strip-types src/lower.mts",
    "build-wasm": "node --experimental-strip-types src/build-wasm.mts",
    "certify": "node --experimental-strip-types src/certify.mts && node --experimental-strip-types src/certify-verify.mts",
    "host:opa": "cargo run --release --manifest-path host/Cargo.toml -- --arm opa",
    "host:regorus": "cargo run --release --manifest-path host/Cargo.toml -- --arm regorus",
    "compare": "node --experimental-strip-types src/compare.mts",
    "differential": "npm run lower && npm run build-wasm && npm run certify && npm run host:opa && npm run host:regorus && npm run compare"
  }
}
