; REWRITE CHECK — a corpus pattern's desugared original vs the form egg
; extracted from its saturated e-class (AstSize cost).
;   pattern = 6ad0d4d5c760a5d6   (\b(guarantees?|comprehensive|best-in-class|industry-leading|world-class|cutting-edge|state-of-the-art|revolutionary|unparalleled|seamless|robust|powerful|enterprise-grade|next-generation|future-proof)\b)
; UNSAT = the rewriting preserved the language.
(set-logic QF_SLIA)

(declare-const s String)

(assert (! (xor (str.in_re s (re.++ (re.union (str.to_re "") (re.++ (re.* (re.union (str.to_re "\u{9}") (re.range " " "~"))) (re.union (str.to_re "\u{9}") (re.range " " "/") (re.range ":" "@") (re.range "[" "^") (str.to_re "`") (re.range "{" "~")))) (re.union (re.++ (str.to_re "guarantee") (re.union (str.to_re "") (str.to_re "s"))) (str.to_re "comprehensive") (str.to_re "best-in-class") (str.to_re "industry-leading") (str.to_re "world-class") (str.to_re "cutting-edge") (str.to_re "state-of-the-art") (str.to_re "revolutionary") (str.to_re "unparalleled") (str.to_re "seamless") (str.to_re "robust") (str.to_re "powerful") (str.to_re "enterprise-grade") (str.to_re "next-generation") (str.to_re "future-proof")) (re.union (str.to_re "") (re.++ (re.union (str.to_re "\u{9}") (re.range " " "/") (re.range ":" "@") (re.range "[" "^") (str.to_re "`") (re.range "{" "~")) (re.* (re.union (str.to_re "\u{9}") (re.range " " "~"))))))) (str.in_re s (re.++ (re.union (str.to_re "") (re.++ (re.* (re.union (str.to_re "\u{9}") (re.range " " "~"))) (re.union (str.to_re "\u{9}") (re.range " " "/") (re.range ":" "@") (re.range "[" "^") (str.to_re "`") (re.range "{" "~")))) (re.union (re.++ (str.to_re "guarantee") (re.union (str.to_re "") (str.to_re "s"))) (str.to_re "enterprise-grade") (str.to_re "next-generation") (str.to_re "seamless") (str.to_re "future-proof") (str.to_re "state-of-the-art") (str.to_re "revolutionary") (str.to_re "robust") (str.to_re "powerful") (str.to_re "industry-leading") (str.to_re "unparalleled") (str.to_re "world-class") (str.to_re "cutting-edge") (str.to_re "comprehensive") (str.to_re "best-in-class")) (re.union (str.to_re "") (re.++ (re.union (str.to_re "\u{9}") (re.range " " "/") (re.range ":" "@") (re.range "[" "^") (str.to_re "`") (re.range "{" "~")) (re.* (re.union (str.to_re "\u{9}") (re.range " " "~")))))))) :named symmetric-difference))
(check-sat)
