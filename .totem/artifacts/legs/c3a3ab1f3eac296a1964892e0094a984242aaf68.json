{
  "schemaVersion": "1.0.0",
  "diffSha": "c3a3ab1f3eac296a1964892e0094a984242aaf68",
  "readAt": "2026-09-04T08:22:00Z",
  "findings": [
    {
      "id": "f16",
      "severity": "MINOR",
      "file": "packages/cli/src/commands/install-hooks.ts",
      "line": 1611,
      "claim": "The decline is reported, so totem doctor's remedy and this command's summary never disagree, both arms sharing one byte-offset predicate.",
      "counterexample": "ownedTrailerByteStart proves byte 0 through the end marker while doctor's staleness gate is totemOwnedBlock (marker to end marker, exclusive), so a hook with a non-decoding byte on its shebang line only yielded doctor 'pass' / 'All 4 hooks installed' alongside installer 'skipped-non-utf8' (executed), and the five user-facing messages all said 'the managed block does not decode' when in that shape the managed block decodes fine and only the preamble does not."
    },
    {
      "id": "f17",
      "severity": "MINOR",
      "file": "packages/cli/src/commands/doctor.test.ts",
      "line": 566,
      "claim": "The negative assertion guards that pre-commit is named only in the re-save clause, never in the bare-install clause.",
      "counterexample": "/totem hook install[^;]*pre-commit[^;]*carried through/ could not match any output of this composer because the attested clause's own parenthetical contains a semicolon, so [^;]* cannot bridge the two - executed against both regressions the comment names, the regex matched neither."
    },
    {
      "id": "f18",
      "severity": "MINOR",
      "file": "packages/cli/src/commands/install-hooks.ts",
      "line": 1605,
      "claim": "The arm reuses the offset the enclosing predicate already proved rather than recomputing the marker scan.",
      "counterexample": "The line directly under that comment was `if (ownedTrailerStart(existing, marker, endMarker) !== undefined)`, which recomputes the scan and is provably always true because isTotemOwnedWithAttestedTrailer is exactly that test plus the attestation, so ownedTrailerStart ran three times per call on the identical string and the branch was dead."
    },
    {
      "id": "f19",
      "severity": "MINOR",
      "file": "packages/cli/src/commands/doctor.ts",
      "line": 318,
      "claim": "Mixed shapes compose one readable clause each.",
      "counterexample": "The nonUtf8 clause was pushed last and ended with its own em-dash tail, immediately followed by the pre-existing appended suffix, so a hook set with both shapes rendered two stacked em-dash clauses about --force in which the second, concerning the appended hook, read as qualifying the first, concerning the non-utf8 one (executed)."
    }
  ],
  "folded": [
    "f16",
    "f17",
    "f18",
    "f19"
  ],
  "verdict": "0 BLOCKING, 0 MATERIAL, 4 MINOR over c3a3ab1f (the skipped-non-utf8 / non-utf8 fold); all four folded in ec7938a9: doctor runs the installer's byte-offset predicate on the attested shape before the staleness compare so a preamble-only bad byte warns, every message names the region the predicate covers, the dead re-scan branch is removed, the re-save clause precedes the --force clause with its tail inside its parenthetical, and the doctor test's negative regex can fail. The leg confirmed exhaustiveness over every GitHookAction consumer, the classification order, the new tests' discrimination per commit, and that no surface still describes this shape as a silent exists. The ladder has converged; a final narrow leg reads ec7938a9."
}