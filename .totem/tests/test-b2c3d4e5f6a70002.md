---
rule: b2c3d4e5f6a70002
file: src/ffi.rs
---

## Should fail

```rust
unsafe {
    ptr::write(dest, src);
}
```

## Should pass

```rust
// SAFETY: dest is valid and aligned, src is a valid value of type T
unsafe {
    ptr::write(dest, src);
}
```
