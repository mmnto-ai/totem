## Lesson — When generating headings from markdown content, use

**Tags:** cli, markdown, ux

When generating headings from markdown content, use a cleaning utility to strip formatting like backticks or bolding. This prevents visual noise in CLI menus and ensures consistent formatting in generated lesson files.
