## Lesson — Raw child process output can be a Buffer or null; adding

**Tags:** typescript, node.js, exec

Raw child process output can be a Buffer or null; adding type guards before string operations like trim() prevents runtime crashes in strictly typed environments.
