## Lesson — A top-level catch-all handler is necessary to ensure

**Tags:** cli, error-handling

A top-level catch-all handler is necessary to ensure that even unhandled promise rejections or internal parser failures return a valid JSON envelope when structured output is enabled.
