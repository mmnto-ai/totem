## Lesson — Use advisory headings for context-blind rules

**Tags:** linting, regex, dx
**Scope:** packages/cli/src/assets/baseline-typescript-new.ts

Rule headings should use advisory language like 'Ensure wrapped' when the underlying regex cannot technically verify surrounding context like try-catch blocks.
