## Lesson — Metadata keys extracted from prose must require a mandatory

**Tags:** regex, validation, parsing

Metadata keys extracted from prose must require a mandatory delimiter to prevent accidental matches on standard sentences starting with keywords. Without a mandatory colon, a line like "Pattern is important" could be incorrectly parsed as a "Pattern" field value.
