## Lesson — Narrow AST-grep constructor patterns

**Tags:** ast-grep, linting, errors
**Scope:** packages/pack-agent-security/**/*.ts, !**/*.test.*, !**/*.spec.*

Generic patterns like 'new $ERROR($$$ARGS)' match all class instantiations including Set or Date; use catch-block context and argument checks to target error wrapping specifically.
