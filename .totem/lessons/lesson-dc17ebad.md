## Lesson — Defining named constants for file permissions (such

**Tags:** security, filesystem, coding-standards

Defining named constants for file permissions (such as 0o644) instead of using inline magic numbers improves code readability and ensures compliance with internal security policies. This makes the intent of file access levels explicit and easier to audit across the codebase.
