## Lesson — Anchor command validation patterns with ^ to prevent hooks

**Tags:** bash, shell-scripting, security

Anchor command validation patterns with `^` to prevent hooks from triggering on arguments that contain the command name as a substring. This ensures the logic only executes for the primary action and avoids false positives during complex command execution.
