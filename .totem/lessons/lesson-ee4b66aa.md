## Lesson — Use 'totem-ignore' comments within string fixtures

**Tags:** testing, linting

Use 'totem-ignore' comments within string fixtures containing intentional errors to prevent the compiler from flagging test data as real issues.
