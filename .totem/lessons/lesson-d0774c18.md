## Lesson — Use file-level locking when updating global workspace

**Tags:** concurrency, registry, node.js

Use file-level locking when updating global workspace registries to prevent race conditions and data corruption during concurrent synchronizations.
