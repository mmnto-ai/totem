## Lesson — Manually joining strings for YAML frontmatter can break

**Tags:** yaml, serialization

Manually joining strings for YAML frontmatter can break the parser if tags contain special characters; use a proper serializer or explicit escaping for quotes and backslashes.
