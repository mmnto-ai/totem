## Lesson — Warnings emitted during batch operations should include

**Tags:** dx, logging

Warnings emitted during batch operations should include an index or a content snippet to help developers identify the specific item that failed. Generic error messages are ambiguous when multiple rules or queries are applied to the same file.
