## Lesson — Per-query error handling in AST engines prevents

**Tags:** ast, typescript, error-handling

Per-query error handling in AST engines prevents environment-specific syntax, such as TypeScript-only nodes, from causing a total failure when analyzing compatible files like JavaScript.
