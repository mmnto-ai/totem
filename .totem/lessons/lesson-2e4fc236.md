## Lesson — Non-deterministic selection of messages when grouping

**Tags:** linting, dx

Non-deterministic selection of messages when grouping violations by location causes flickering PR comments; sorting by a stable identifier like a hash ensures consistent summaries.
