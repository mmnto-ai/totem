## Lesson — Placing imports inside command function bodies prevents

**Tags:** nodejs, performance, cli

Placing imports inside command function bodies prevents the runtime from loading heavy dependencies during the initial CLI startup phase. This ensures the tool remains responsive for all commands by only loading modules relevant to the specific action being executed.
