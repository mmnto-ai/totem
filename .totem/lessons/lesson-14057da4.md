## Lesson — Use specific validation ignore lists instead of global

**Tags:** configuration, indexing, validation

Use specific validation ignore lists instead of global ignore patterns when files should be excluded from linting but must remain accessible for vector search or AI context. Global ignores often remove files from indexing entirely, breaking retrieval-augmented features.
