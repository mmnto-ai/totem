## Lesson — Consolidate overlapping dynamic imports

**Tags:** typescript, dx
**Scope:** packages/cli/src/commands/doctor.ts

Consolidate multiple dynamic imports from the same module into a single destructure to improve code clarity and reduce execution overhead.
