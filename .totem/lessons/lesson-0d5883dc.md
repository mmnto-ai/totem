## Lesson — Ensure JSON output paths mirror the success/failure logic

**Tags:** cli, api-design

Ensure JSON output paths mirror the success/failure logic of human-readable paths to prevent silent failures where an empty result is a success in JSON but an error in the UI.
