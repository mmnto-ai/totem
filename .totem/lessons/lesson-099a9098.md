## Lesson — Pin timezones in child process tests

**Tags:** testing, timezone, ci
**Scope:** tools/docs-transforms.test.cjs

Timezone-dependent date calculations can pass locally but hide bugs in different runner environments. Spawning a child process with a pinned timezone environment variable guarantees consistent mutation testing and prevents timezone-related test drift.
