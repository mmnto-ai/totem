## Lesson — Use discriminated unions for resolution results

**Tags:** typescript, api-design
**Scope:** packages/core/src/**/*.ts

Returning a StrategyRootStatus union instead of nullable fields forces callers to handle the 'unresolved' state explicitly, preventing runtime type assertions.
