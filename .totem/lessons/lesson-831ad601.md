## Lesson — In systems where content hashes are derived from headings,

**Tags:** identifiers, hashing, maintenance

In systems where content hashes are derived from headings, updating a heading to improve readability will break downstream mappings and external references. If headings are used as identifiers, plan for a migration strategy before performing bulk style updates.
