## Lesson — The text-embedding-004 identifier is frequently unavailable

**Tags:** gemini, llm, embeddings

The `text-embedding-004` identifier is frequently unavailable or invalid for Gemini integrations; use `gemini-embedding-2-preview` as the correct identifier. This prevents initialization failures for new users where the stale model name is rejected by the API.
