## Lesson — Prevent silent fallback to local context

**Tags:** security, cli, parsing
**Scope:** packages/cli/src/**/*.ts, !**/*.test.*, !**/*.spec.*

When parsing external URLs that contain resource scopes, always fetch from the named scope rather than falling back to the working directory. This prevents silent anchoring on incorrect resources of the same ID in the local repository.
