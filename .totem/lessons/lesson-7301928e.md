## Lesson — Using 'pnpm run version' instead of the bare 'pnpm version'

**Tags:** pnpm, release

Using 'pnpm run version' instead of the bare 'pnpm version' command is necessary to correctly trigger the Changesets workflow and update changelogs.
