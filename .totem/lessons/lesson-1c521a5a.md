## Lesson — Git hooks should utilize fast, deterministic linting tools

**Tags:** git, performance, workflow

Git hooks should utilize fast, deterministic linting tools rather than slower AI-powered review tools to avoid workflow friction. This prevents non-deterministic AI outputs from blocking the developer's "fast path" during operations like a git push.
