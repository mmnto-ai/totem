## Lesson — Utilize jq instead of standard string manipulation tools

**Tags:** shell, devops

Utilize `jq` instead of standard string manipulation tools like `grep` or `sed` to ensure reliable data extraction from structured JSON configuration files.
