## Lesson — Validate declared schema versions

**Tags:** schema, validation, versioning, cli
**Scope:** packages/cli/**/*.ts, !**/*.test.*

When importing external data files, validate their declared schema version for compatibility before overwriting them with the writer's current version. Unconditional relabeling can cause the system to silently accept and store unsupported major versions under inaccurate labels.
