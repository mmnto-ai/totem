## Lesson — Avoid broad negative tokens like \bno\b when detecting

**Tags:** regex, nlp

Avoid broad negative tokens like `\bno\b` when detecting developer pushback to prevent false positives on phrases like 'No worries, fixed'.
