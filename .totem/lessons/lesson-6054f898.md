## Lesson — Avoid generic empty-array AST-grep patterns

**Tags:** ast-grep, linting
**Scope:** packages/pack-agent-security/**/*.ts

Generic patterns like `const $VAR = []` are too broad and trigger on all empty array declarations; use more specific identifiers to avoid high false-positive rates.
