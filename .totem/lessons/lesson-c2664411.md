## Lesson — Assert raw strings for byte-identity

**Tags:** testing, serialization
**Scope:** packages/mcp/src/**/*.test.*

Structural equality checks via JSON.parse fail to detect formatting or property-order drifts. Assert against raw serialized strings when the contract requires byte-identical legacy output.
