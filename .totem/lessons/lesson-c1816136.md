## Lesson — Apply guards to manual patterns

**Tags:** compiler, validation, dx
**Scope:** packages/core/src/compile-lesson.ts

Manual rule authoring pipelines must include the same validation guards as automated ones. This ensures that manually created patterns do not bypass critical checks like the self-suppression guard.
