## Lesson — Keep internal cache or flag filenames unchanged

**Tags:** refactoring, compatibility

Keep internal cache or flag filenames unchanged during command renames to prevent breaking existing local state while updating user-facing logs.
