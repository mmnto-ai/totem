## Lesson — Restricting dynamic imports to CLI command entry points

**Tags:** performance, curated
**Pattern:** \bimport\s*\(
**Engine:** regex
**Scope:** packages/core/src/**/*.ts, packages/cli/src/adapters/**/*.ts, packages/cli/src/utils.ts, !**/*.test.ts
**Severity:** warning

Dynamic imports are restricted to CLI command entry points. Use standard top-level imports in utility and adapter layers.
