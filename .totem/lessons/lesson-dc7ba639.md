## Lesson — Use major-minor cohorts for manifest versioning

**Tags:** schema, versioning, dx
**Scope:** packages/core/**/*.ts

Stamping manifests with a major.minor 'cohort' allows the engine to detect stale configurations while maintaining compatibility across patch-level engine updates.
