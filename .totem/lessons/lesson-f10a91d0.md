## Lesson — Use canonical API terms in documentation

**Tags:** documentation, standards
**Scope:** docs/**/*.md

User-facing documentation must use canonical, publicly documented API terms (e.g., Anthropic's 'thinking') to prevent user confusion. This applies even when internal code uses different abstraction names like 'adaptive_thinking'.
