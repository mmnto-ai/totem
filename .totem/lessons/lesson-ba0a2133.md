## Lesson — Concatenating severity strings with comment bodies

**Tags:** logic, categorization

Concatenating severity strings with comment bodies before keyword matching causes misclassification, such as 'minor' severity labels accidentally triggering 'nit' keyword matches.
