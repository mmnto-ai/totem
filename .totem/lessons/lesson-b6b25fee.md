## Lesson — Prevent silent rule compilation failures

**Tags:** architecture, compilation, validation

A multi-layer fallback architecture with explicit failure reporting prevents the compiler from silently producing zero-match rules from valid inputs, a failure mode identified in stress tests.
