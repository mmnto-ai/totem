## Lesson — Fail closed on incomplete pagination windows

**Tags:** graphql, pagination, security
**Scope:** packages/**/*.ts, !**/*.test.*, !**/*.spec.*

When evaluating evidence across paginated connections, an incomplete window that exceeds the fetch limit must fail-closed if no evidence is found, rather than assuming no evidence exists.
