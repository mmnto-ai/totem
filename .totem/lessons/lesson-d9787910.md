## Lesson — Fail closed on shared suite duplicates

**Tags:** github, ci, error-handling
**Scope:** packages/core/src/**/*.ts, !**/*.test.*, !**/*.spec.*

When multiple same-named runs of a single producer exist within the same check suite, they cannot be reliably ordered or collapsed. These ambiguous states must fail closed rather than risk an incorrect success rollup.
