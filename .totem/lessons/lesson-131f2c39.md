## Lesson — Check for cancellation signals (like Clack's isCancel)

**Tags:** cli, ux

Check for cancellation signals (like Clack's isCancel) after every user prompt to ensure the process exits gracefully on user interrupts like Ctrl+C.
