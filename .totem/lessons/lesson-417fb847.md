## Lesson — Removing stdio from execution options and hardcoding pipe

**Tags:** node.js, exec, typescript

Removing stdio from execution options and hardcoding pipe mode ensures that return values consistently match string-based type contracts and normalization logic.
