## Lesson — Pass generated outputs through a schema or sorter to ensure

**Tags:** git, dx, json

Pass generated outputs through a schema or sorter to ensure consistent property ordering across runs. This prevents unnecessary Git diff noise caused by non-deterministic field ordering in automated commits.
