## Lesson — Verify ownership before removing config entries

**Tags:** cli, configuration
**Scope:** packages/cli/src/commands/**/*.ts, !**/*.test.*, !**/*.spec.*

Use specific markers or ownership predicates rather than broad substring matching when scrubbing configuration entries to prevent the accidental deletion of user-authored content.
