## Lesson — Forward configuration to multi-layer resolvers

**Tags:** architecture, dx
**Scope:** packages/cli/src/utils/governance.ts

Programmatic consumers must explicitly pass the configuration object to resolvers to ensure that user-defined overrides are respected across all precedence layers.
