## Lesson — Trailing wildcards on filename prefixes (e.g., compiler*)

**Tags:** glob, github-actions

Trailing wildcards on filename prefixes (e.g., `compiler*`) effectively group related flat files like tests and schemas without requiring a directory structure.
