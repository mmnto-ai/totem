## Lesson — Avoid broad heading-based filtering

**Tags:** extraction, logic, filtering
**Scope:** packages/cli/src/commands/extract-shared.ts

Lesson heading matching in retirement ledgers should be specific rather than broad. Overly broad matching can lead to false positives where valid new lessons are incorrectly filtered out.
