## Lesson — Instrument metrics only after validation

**Tags:** instrumentation, metrics
**Scope:** packages/cli/src/commands/**/*.ts

Recording events before validating options (like raw mode) or parsing verdicts can artificially inflate denominators and prematurely consume correlation tokens on skipped runs.
