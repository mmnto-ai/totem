## Lesson — Ensure linting occurs as the final step of every small task

**Tags:** workflow, linting, productivity

Ensure linting occurs as the final step of every small task cycle rather than once at the end of a feature implementation. Frequent validation prevents linting errors from accumulating, which are significantly more time-consuming to resolve in bulk.
