## Lesson — Git log entries for closed-but-unshipped issues can cause

**Tags:** llm, documentation, git

Git log entries for closed-but-unshipped issues can cause persistent LLM hallucinations even if prompt instructions or glossaries explicitly forbid their inclusion. Stripping these references at the data level before and after LLM processing is more effective than relying on context-layer overrides.
