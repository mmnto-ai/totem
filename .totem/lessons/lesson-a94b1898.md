## Lesson — Bump major version for schema changes

**Tags:** changesets, semver, mcp
**Scope:** .changeset/*.md

Breaking changes to public API shapes, such as switching a field to a discriminated union, must be marked as a major version bump in changesets to ensure downstream consumers handle the new structure.
