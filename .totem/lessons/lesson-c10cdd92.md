## Lesson — Manually editing compiled rule files

**Tags:** totem-compile, automation, hotfix

Manually editing compiled rule files like `compiled-rules.json` is a temporary fix that is overwritten during the next build cycle. To permanently resolve issues, you must modify the source lessons or implement deduplication logic within the compiler itself.
