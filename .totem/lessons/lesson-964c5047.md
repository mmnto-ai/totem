## Lesson — Scrub Git environment variables in subprocesses

**Tags:** git, subprocess, environment
**Scope:** packages/**/*.ts, !**/*.test.*, !**/*.spec.*

Git subprocesses spawned within hooks can inherit environment variables like GIT_DIR and query the wrong repository index. Scrubbing these location variables ensures that probes accurately query the intended local path.
