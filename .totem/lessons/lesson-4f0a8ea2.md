## Lesson — Using non-unique fields like timestamps as keys

**Tags:** automation, scripts, mapping

Using non-unique fields like timestamps as keys for automated data mapping leads to cross-contamination and incorrect data associations. Automation scripts should rely on content-based hashes or UUIDs when merging or updating data across multiple files.
