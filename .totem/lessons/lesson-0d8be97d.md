## Lesson — Rule metadata or headings containing the literal pattern

**Tags:** ci, guardrails, regex

Rule metadata or headings containing the literal pattern they guard against can trigger the rule on themselves. Renaming or obfuscating these strings prevents false positives in automated CI checks.
