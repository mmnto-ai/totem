## Lesson — Normalizing shallow patterns like .ts to /.ts ensures

**Tags:** globs, portability

Normalizing shallow patterns like `*.ts` to `**/*.ts` ensures consistent behavior across tools like SARIF and picomatch that may not treat non-prefixed patterns as recursive.
