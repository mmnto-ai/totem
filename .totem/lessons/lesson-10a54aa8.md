## Lesson — Sync documentation tables with code maps

**Tags:** testing, documentation, synchronization
**Scope:** packages/cli/**/*.test.ts

When documentation tables must align exactly with static code configuration maps, write a synchronization unit test to hold the two as a strict equality to prevent configuration drift.
