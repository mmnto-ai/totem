## Lesson — Limit manual edits to lifecycle changes

**Tags:** totem, dx, workflow
**Scope:** .totem/compiled-rules.json

Direct edits to compiled rule files are only permissible for lifecycle state updates like archiving; pattern or message corrections must flow through the source lesson compiler.
