## Lesson — When forbidding a native module or pattern, the rule

**Tags:** architecture, linting

When forbidding a native module or pattern, the rule must explicitly exclude the directory containing the approved wrapper implementation. This prevents circular linting failures while enforcing the pattern across the rest of the codebase.
