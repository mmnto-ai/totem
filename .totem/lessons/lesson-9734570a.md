## Lesson — The parseInt function accepts strings with trailing

**Tags:** typescript, validation

The `parseInt` function accepts strings with trailing non-numeric characters; use a strict regex like `/^\d+$/` to ensure identifiers are purely numeric before conversion.
