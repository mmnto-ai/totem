## Lesson — Every totem spec output must include linting, shielding,

**Tags:** totem-spec, linting, automation

Every `totem spec` output must include linting, shielding, and execution verification as the final implementation steps. This ensures that AI-generated code is immediately validated against project constraints before being considered complete.
