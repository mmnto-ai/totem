## Lesson — Stabilize host-dependent integration tests

**Tags:** testing, mcp
**Scope:** packages/mcp/src/**/*.ts

Use conditional runtime skips for integration tests that depend on optional host-level resources like strategy clones to prevent false failures.
