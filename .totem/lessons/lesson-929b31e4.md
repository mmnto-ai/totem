## Lesson — Core library modules should use optional callbacks

**Tags:** architecture, logging

Core library modules should use optional callbacks like onWarn instead of hardcoded console methods to remain agnostic of the execution environment.
