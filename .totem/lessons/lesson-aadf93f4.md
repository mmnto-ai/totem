## Lesson — Updating comment-matching regex to support trailing inline

**Tags:** regex, dx

Updating comment-matching regex to support trailing inline directives ensures that suppressions are captured even when developers append them to code lines rather than using full-line comments.
