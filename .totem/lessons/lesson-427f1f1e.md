## Lesson — YAML frontmatter should take precedence over inline tags

**Tags:** architecture, metadata, yaml

YAML frontmatter should take precedence over inline tags to ensure a single source of truth and maintain consistency across different parsing strategies.
