## Lesson — Ensure ignore patterns filter the actual diff content

**Tags:** ai-shield, git, submodules

Ensure ignore patterns filter the actual diff content of AI-generated changes rather than just file paths. This prevents false positives in protected areas like submodules where automated strategies might otherwise trigger unintended warnings.
