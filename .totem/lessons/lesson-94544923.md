## Lesson — Silently ignoring Git checkout errors in cleanup blocks

**Tags:** git, ux, cli

Silently ignoring Git checkout errors in cleanup blocks can leave users stranded on temporary branches without notice. Always log a warning with recovery instructions if a branch restoration fails during a command's finalization.
