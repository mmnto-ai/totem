## Lesson — In core packages, a small, tested custom implementation

**Tags:** style, curated
**Pattern:** \b(import|require).+['"]micromatch['"]
**Engine:** regex
**Scope:** packages/core/**/*
**Severity:** warning

In core packages, prefer a small custom implementation over external libraries like 'micromatch' to keep the dependency graph lean.
