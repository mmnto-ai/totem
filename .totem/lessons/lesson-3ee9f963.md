## Lesson — Order strategy root resolution precedence

**Tags:** architecture, configuration
**Scope:** packages/core/src/**/*.ts

A four-layer precedence system (Env > Config > Sibling > Submodule) allows flexible strategy root resolution across local development, CI, and legacy environments.
