## Lesson — The fileGlobs array can contain objects for ast-grep rules,

**Tags:** typescript, glob, ast-grep

The fileGlobs array can contain objects for ast-grep rules, so utility functions must verify entries are strings before calling string-specific methods like startsWith.
