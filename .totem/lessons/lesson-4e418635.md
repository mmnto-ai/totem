## Lesson — Refresh manifests on pure input drift

**Tags:** architecture, integrity, cli
**Scope:** packages/cli/src/commands/**/*.ts, !**/*.test.*

Ensure manifest files are refreshed when input hashes change even if the primary output content remains identical. Failure to do so can wedge integrity checks, such as pre-push hooks, that rely on strict manifest-to-input alignment.
