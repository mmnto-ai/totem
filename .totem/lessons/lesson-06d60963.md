## Lesson — Avoid overstating validation in safety invariants

**Tags:** documentation, dx
**Scope:** packages/**/*.ts, !**/*.test.*, !**/*.spec.*

Distinguish between formal schema validation and simple type narrowing in comments; use 'SAFETY INVARIANT' to document the latter without overstating the level of check.
