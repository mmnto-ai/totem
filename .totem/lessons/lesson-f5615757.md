## Lesson — Contain blast radius with per-rule try-catch

**Tags:** architecture, reliability
**Scope:** packages/core/**/*.ts, !**/*.test.*, !**/*.spec.*

Executing individual rules within a try-catch block inside batch loops prevents a single malformed rule from crashing the entire engine execution.
