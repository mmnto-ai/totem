## Lesson — Warn on skipped configured resources

**Tags:** cli, dx, logging
**Scope:** packages/cli/src/commands/search.ts

Log explicit warnings when skipping configured linked repositories due to missing dependencies like embedding providers to ensure users are aware of partial failures.
