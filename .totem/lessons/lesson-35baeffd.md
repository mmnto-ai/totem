## Lesson — Manually prune retired submodule artifacts

**Tags:** git, dx

Git does not automatically prune submodule directories or internal metadata after removal, so manual deletion is required to fully clean local clones.
