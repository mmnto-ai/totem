## Lesson — Avoid direct stdio writes in MCP

**Tags:** mcp, logging, protocol
**Scope:** packages/mcp/**/*.ts

Direct writes to process.stderr or process.stdout in MCP servers can corrupt the stdio transport protocol. Route all diagnostic logs through structured synthetic entries or transport-safe channels.
