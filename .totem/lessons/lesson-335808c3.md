## Lesson — Avoid direct stdio writes in MCP

**Tags:** mcp, logging, protocol
**Scope:** packages/mcp/**/*.ts

Non-protocol writes to process.stdout from a stdio-transport MCP server corrupt the protocol stream; stderr is permitted for server diagnostics. Route diagnostic logs through stderr or structured transport-safe channels — and through the transport's own logging mechanism on non-stdio transports — never through stdout.
