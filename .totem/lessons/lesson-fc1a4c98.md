## Lesson — Creating changeset Markdown files manually

**Tags:** release, dx

Creating changeset Markdown files manually in the '.changeset/' directory avoids execution failures common with interactive CLI tools when stdin is piped.
