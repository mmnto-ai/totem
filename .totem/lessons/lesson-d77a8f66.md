## Lesson — Injecting manual content into all prompt contexts causes

**Tags:** llm, prompt-engineering, documentation

Injecting manual content into all prompt contexts causes LLMs to redundantly include that content in every generated document regardless of relevance. Scoping content injection to specific target files prevents pollution of internal documentation with sections intended only for user-facing docs.
