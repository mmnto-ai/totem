## Lesson — Use maxRetries and retryDelay in fs.rmSync calls

**Tags:** node.js, windows, testing

Use `maxRetries` and `retryDelay` in `fs.rmSync` calls during test teardown to prevent `ENOTEMPTY` errors on Windows caused by filesystem lag or locks.
