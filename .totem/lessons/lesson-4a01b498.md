## Lesson — Always stage specific files instead of using 'git add -A'

**Tags:** git, automation

Always stage specific files instead of using 'git add -A' in automated workflows. This prevents the accidental inclusion of unrelated local changes in an agent-generated atomic commit.
