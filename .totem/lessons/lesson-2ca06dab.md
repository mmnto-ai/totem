## Lesson — Adding 'edited' to pull_request trigger types ensures

**Tags:** github-actions, ci

Adding 'edited' to pull_request trigger types ensures that modifying a PR title to add a bypass string (like [UPDATE-RULES]) creates a fresh workflow run with updated context.
