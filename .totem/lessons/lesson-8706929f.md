## Lesson — Git's porcelain output uses C-style escaping for paths

**Tags:** git, parsing

Git's porcelain output uses C-style escaping for paths containing special characters or quotes. These must be explicitly decoded (e.g., handling backslashes and quotes) to obtain the actual filesystem path.
