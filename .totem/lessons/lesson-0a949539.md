## Lesson — When synthesizing rules from diff hunks, ensure

**Tags:** regex, compiler, git

When synthesizing rules from diff hunks, ensure unified-diff prefixes (+, -, space) are stripped to prevent 'dead' patterns that only match patch files instead of source code.
