## Lesson — Narrow hook execution using if fields

**Tags:** claude-code, hooks, performance
**Scope:** .claude/settings.json

Use the `if` field in Claude Code hook configurations to filter commands before spawning a subshell, preventing measurable latency during high-frequency tool use cycles.
