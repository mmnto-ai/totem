## Lesson — Pure core utilities may use empty catch blocks

**Tags:** typescript, error-handling

Pure core utilities may use empty catch blocks as a last-resort safety net if validation occurs upstream and the package lacks logging dependencies.
