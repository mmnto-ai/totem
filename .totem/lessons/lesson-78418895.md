## Lesson — Using a prefix glob like src/compiler* effectively targets

**Tags:** github-actions, devops

Using a prefix glob like `src/compiler*` effectively targets multiple related flat files (source, tests, and schemas) when they share a naming convention but lack a common subdirectory.
