## Lesson — Using a unified "Totem Error" tag in error logs instead

**Tags:** logging, conventions, cli

Using a unified "Totem Error" tag in error logs instead of command-specific identifiers ensures consistent output and filtering across the entire tool suite. Centralizing error identification prevents fragmented log styles and adheres to project-wide observability standards.
