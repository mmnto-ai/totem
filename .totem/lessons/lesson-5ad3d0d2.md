## Lesson — Introducing new event types to a shared ledger can silently

**Tags:** analytics, ledger

Introducing new event types to a shared ledger can silently alter existing bypass or usage metrics. Explicitly filtering event types in metric calculations preserves original semantics when the data model expands.
