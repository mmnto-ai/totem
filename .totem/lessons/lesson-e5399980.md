## Lesson — Always include POSIX shell rules

**Tags:** shell, architecture, security
**Scope:** packages/cli/src/commands/init.ts

POSIX shell rules are included in all baseline packs because the tool's own git hooks are implemented in shell, ensuring infrastructure safety across all projects.
