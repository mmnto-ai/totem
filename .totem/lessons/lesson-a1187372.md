## Lesson — Avoid opinionated policy in core runners

**Tags:** architecture, cli, ci
**Scope:** packages/cli/src/commands/**/*.ts, !**/*.test.*

Core runners should avoid enforcing specific CI policies like minimum rule counts, allowing consumers to implement their own guardrails externally.
