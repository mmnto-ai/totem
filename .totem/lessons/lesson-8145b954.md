## Lesson — Check quote status of shell operators

**Tags:** shell, parsing, security
**Scope:** packages/**/*.ts, !**/*.test.*, !**/*.spec.*

Shell redirection operators are evaluated based on the quoting of the operator prefix, requiring tokenization to track character escape offsets to avoid stripping valid command arguments.
