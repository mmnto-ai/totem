## Lesson — Running 'totem docs' immediately after 'totem extract'

**Tags:** release, documentation

Running 'totem docs' immediately after 'totem extract' is required during release flows to ensure canonical documentation stays in sync with newly captured knowledge.
