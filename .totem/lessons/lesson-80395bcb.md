## Lesson — Reporting malformed records via an onWarn callback instead

**Tags:** observability, telemetry

Reporting malformed records via an onWarn callback instead of silent catch blocks allows debugging of corrupted NDJSON streams without interrupting the main execution flow.
