## Lesson — Shipping a tool with pre-compiled default rules prevents

**Tags:** onboarding, dx, configuration

Shipping a tool with pre-compiled default rules prevents "no configuration found" errors during the initial user experience. This ensures that users see immediate value without needing to configure API keys or custom rules on their first run.
