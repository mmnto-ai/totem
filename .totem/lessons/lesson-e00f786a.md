## Lesson — Differentiating between */.ext and dir/*.ext in LLM prompts

**Tags:** llm-prompting, globs

Differentiating between `**/*.ext` and `dir/*.ext` in LLM prompts prevents ambiguous output and ensures the model generates portable glob patterns that work across different environments.
