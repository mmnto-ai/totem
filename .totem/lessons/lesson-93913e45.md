## Lesson — Account for CI runner process spawn overhead

**Tags:** testing, ci, performance
**Scope:** packages/**/*.test.ts

Tests that spawn external processes (like shell scripts or git commands) run significantly slower on Windows and macOS CI runners than on local machines. These suites require dedicated, elevated timeouts (e.g., 30–60 seconds) to prevent transient, load-induced CI failures.
