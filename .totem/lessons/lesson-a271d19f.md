## Lesson — Apply Zod validation primarily at system boundaries, such

**Tags:** typescript, validation, zod

Apply Zod validation primarily at system boundaries, such as configuration files and API inputs, rather than for internal data structures. Overusing Zod for small internal parsers or data transformers adds unnecessary overhead and complexity to the codebase.
