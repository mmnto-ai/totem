## Lesson — Gate auto-capture behind codebase verifiers

**Tags:** architecture, llm, quality-control
**Scope:** packages/cli/src/**/*.ts, !**/*.test.*, !**/*.spec.*

Strategic features that emit automated content should remain opt-in until multi-stage classifiers and codebase-aware verifiers can prevent low-quality outputs.
