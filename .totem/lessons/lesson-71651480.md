## Lesson — Verify ownership before scrubbing config

**Tags:** cli, eject, json
**Scope:** packages/cli/src/commands/**/*.ts, !**/*.test.*, !**/*.spec.*

When removing configuration entries during eject, use explicit markers to verify tool ownership instead of substring matching to prevent accidental deletion of user-defined hooks.
