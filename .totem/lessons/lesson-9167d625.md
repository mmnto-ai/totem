## Lesson — Use granular assertions rather than snapshots for testing

**Tags:** style, curated
**Pattern:** \.toMatch(Inline)?Snapshot\s*\(
**Engine:** regex
**Scope:** \*\*/*.test.ts, **/\*.test.js, **/_.spec.ts, \*\*/_.spec.js
**Severity:** warning

Use granular assertions rather than snapshots for testing system prompts to avoid brittle tests caused by minor prose changes
