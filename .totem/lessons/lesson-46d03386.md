## Lesson — Bypass id-match lint via bracket notation

**Tags:** linting, typescript, dx
**Scope:** packages/cli/src/index-lite.ts

Using bracket notation for object keys can satisfy id-match lint rules for specific field names without requiring explicit eslint-disable comments.
