## Lesson — Instead of hardcoding .git/hooks, use 'git rev-parse

**Tags:** git, cli

Instead of hardcoding .git/hooks, use 'git rev-parse --git-path hooks' to support custom hook paths and worktrees correctly.
