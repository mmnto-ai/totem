## Lesson — Strip Markdown formatting from finding descriptions

**Tags:** git, llm, parsing

Strip Markdown formatting from finding descriptions before including them in Git commit summaries. This prevents malformed conventional commits that could break CI or downstream parsing.
