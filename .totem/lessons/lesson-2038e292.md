## Lesson — Verify escaped characters in global renames

**Tags:** regex, testing, automation
**Scope:** packages/**/*.ts, !**/*.test.*, !**/*.spec.*

Standard sed substitutions often miss escaped characters in regex literals (e.g., @scope\/name); use multi-pass scripts to ensure test assertions remain valid after a package rename.
