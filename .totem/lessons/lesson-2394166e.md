## Lesson — Leverage existing pipeline data for zero-cost telemetry

**Tags:** telemetry, performance, ast
**Scope:** packages/**/*.ts, !**/*.test.*, !**/*.spec.*

Utilize data already generated in earlier pipeline stages, such as AST context, to implement new telemetry without introducing additional parsing or API overhead.
