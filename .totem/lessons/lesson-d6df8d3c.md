## Lesson — Separate fast, pattern-based checks into a "lint" command

**Tags:** architecture, dx, naming

Separate fast, pattern-based checks into a "lint" command while reserving "shield" for thorough AI-powered analysis. Naming commands based on user intent (speed vs. depth) rather than underlying implementation (static rules vs. LLM) reduces cognitive load and aligns with industry-standard terminology.
