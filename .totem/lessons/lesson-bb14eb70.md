## Lesson — Avoid using non-unique fields like headings to identify

**Tags:** data-integrity, pruning, identification

Avoid using non-unique fields like headings to identify items for deletion or pruning operations. Matching by full raw content or unique hashes prevents accidental data loss when multiple items share the same metadata.
