## Lesson — Core packages should use an injectable logger interface

**Tags:** architecture, logging

Core packages should use an injectable logger interface rather than direct console calls to allow consumers like CLIs to control output routing according to architectural boundaries.
