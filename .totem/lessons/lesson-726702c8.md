## Lesson — git ls-files with relative paths fails from subdirs

**Tags:** shell, git, automation

Git commands like `ls-files` using relative paths can produce inconsistent results or fail when executed from subdirectories. Using `git -C "$REPO_ROOT"` or absolute paths ensures the script behaves correctly regardless of the caller's current working directory.
