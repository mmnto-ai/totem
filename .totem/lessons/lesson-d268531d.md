## Lesson — GitHub comments are capped at 65,535 characters; truncating

**Tags:** github-api, markdown

GitHub comments are capped at 65,535 characters; truncating both total table rows and individual cell strings prevents silent API failures on large linting outputs.
