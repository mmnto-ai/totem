## Lesson — Align probe timeouts with production budgets

**Tags:** architecture, dx
**Scope:** packages/**/*.ts, !**/*.test.*, !**/*.spec.*

Aligning diagnostic probe timeouts with existing production values prevents internal divergence and ensures consistent behavior between troubleshooting tools and runtime logic.
