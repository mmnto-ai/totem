## Lesson — Use asymmetric keyword matching for triage

**Tags:** triage, regex, nlp
**Scope:** packages/cli/src/parsers/triage-severity-mapper.ts

Apply word-boundary constraints specifically to low-severity categories like NITS to prevent false positives, while retaining substring matching for high-severity categories to catch all inflections.
