## Lesson — Store the actual resolved vector size in the registry

**Tags:** embeddings, metadata

Store the actual resolved vector size in the registry rather than 'auto' placeholders to provide accurate diagnostic information about the embedding configuration.
