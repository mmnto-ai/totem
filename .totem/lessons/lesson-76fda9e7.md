## Lesson — When generating reports based on current Git status,

**Tags:** git, io

When generating reports based on current Git status, capture the deterministic state before writing the output file. This prevents the report itself from being included in the snapshot of uncommitted changes.
