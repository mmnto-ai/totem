## Lesson — When extracting data from third-party CLI summaries using

**Tags:** testing, vitest, shell

When extracting data from third-party CLI summaries using grep or awk, implement a safe fallback like ${VAR:-all} to handle unexpected output format changes gracefully.
