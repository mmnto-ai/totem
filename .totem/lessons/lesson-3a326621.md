## Lesson — Constructing shell commands with string interpolation

**Tags:** cli, node, security

Constructing shell commands with string interpolation is vulnerable to paths containing spaces or metacharacters; use argument arrays with spawnSync for robustness.
