## Lesson — Even when the MCP SDK defaults to stdio, explicitly

**Tags:** mcp, configuration, dx

Even when the MCP SDK defaults to `stdio`, explicitly declaring the transport type in tool configurations ensures consistency and reduces ambiguity for different client implementations.
