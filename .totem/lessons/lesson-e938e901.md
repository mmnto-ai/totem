## Lesson — When API adapters lack explicit 'isResolved' flags, use

**Tags:** github-api, heuristics

When API adapters lack explicit 'isResolved' flags, use text-based reply patterns (e.g., 'Fixed' or 'Dismissed') to infer the state of a discussion thread.
