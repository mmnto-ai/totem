## Lesson — Require specific names for failing tests (e.g., "rejects

**Tags:** tdd, testing, prompt-engineering

Require specific names for failing tests (e.g., "rejects empty catch blocks") instead of generic descriptions like "works correctly" during task planning. This forces the agent to define a concrete failure condition that proves the implementation actually solves the intended problem.
