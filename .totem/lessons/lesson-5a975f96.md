## Lesson — Providing an explicit command glossary in system prompts

**Tags:** prompts, documentation, context-clarity

Providing an explicit command glossary in system prompts prevents LLMs from conflating deterministic tools with AI-powered alternatives. This prevents persistent documentation errors that can recur across multiple release cycles when models misinterpret command roles.
