## Lesson — Using 'git rev-parse --show-toplevel' in hooks ensures

**Tags:** git, hooks, bash

Using 'git rev-parse --show-toplevel' in hooks ensures that state files are correctly located regardless of which subdirectory the git command was issued from.
