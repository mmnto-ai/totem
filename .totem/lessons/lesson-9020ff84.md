## Lesson — Explicitly validate the existence of embedder

**Tags:** configuration, validation, embeddings

Explicitly validate the existence of embedder configurations in linked projects before attempting a connection to prevent runtime crashes. In multi-repo environments, you cannot assume all linked nodes share the same maturity level or configuration requirements.
