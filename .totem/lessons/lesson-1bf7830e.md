## Lesson — Using git merge-base --is-ancestor allows a tool to verify

**Tags:** git, caching

Using `git merge-base --is-ancestor` allows a tool to verify if a previous success flag is still valid for the current HEAD, even after rebases or new commits.
