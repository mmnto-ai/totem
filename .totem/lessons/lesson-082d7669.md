## Lesson — Avoid spread push for large arrays

**Tags:** javascript, performance
**Scope:** operations-local/gate5/mig-harness.mjs

Using the spread operator to push elements into an array (e.g., `arr.push(...largeArray)`) can exceed V8's call stack size limit and cause stack overflows if the array is large. Use iterative methods or direct Set additions instead.
