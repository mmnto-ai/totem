## Lesson — Lazy-load heavy configurations and services only

**Tags:** cli, dx, performance

Lazy-load heavy configurations and services only when a specific action is confirmed by the user. This prevents the entire CLI session from aborting due to configuration errors during unrelated tasks.
