## Lesson — Gemini is preferred over OpenAI for embeddings because it

**Tags:** embeddings, gemini, retrieval

Gemini is preferred over OpenAI for embeddings because it supports task-type awareness, which yields superior retrieval quality in RAG applications. This prioritization ensures the system defaults to the highest-quality provider when multiple API keys are present.
