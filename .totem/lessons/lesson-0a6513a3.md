## Lesson — Include file paths in warning messages

**Tags:** observability, dev-experience

Warning messages should include rich metadata such as file paths and query identifiers to remain actionable during batch runs. Without this context, it is difficult to determine which specific file or rule caused a non-fatal failure when processing large datasets.
