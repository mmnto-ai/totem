## Lesson — Export interfaces for custom error fields

**Tags:** typescript, dx
**Scope:** packages/core/src/sys/**/*.ts, !**/*.test.*

Export interfaces for additional fields attached to custom Errors to allow downstream consumers to perform type-safe error handling without casting to any.
