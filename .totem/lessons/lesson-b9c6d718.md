## Lesson — File-level comments require a higher similarity threshold

**Tags:** algorithms, deduplication

File-level comments require a higher similarity threshold (e.g., 0.8 Jaccard) than line-proximate comments to prevent merging distinct architectural feedback.
