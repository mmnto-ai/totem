## Lesson — Using runtime binary resolution for hooks instead

**Tags:** architecture, hooks, dx

Using runtime binary resolution for hooks instead of hardcoded paths enables language-agnostic support and simplifies the distribution of generated helper scripts.
