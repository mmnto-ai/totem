## Lesson — Sampling a single record to compare its stored vector

**Tags:** lancedb, embeddings, vector-database

Sampling a single record to compare its stored vector length against the current embedder is a reliable way to detect provider changes (e.g., OpenAI to Gemini). This proactive check prevents cryptic runtime errors and allows the store to trigger a clean auto-healing cycle when embedding dimensions no longer match the index.
