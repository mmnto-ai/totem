## Lesson — Use negative filters for legacy compatibility

**Tags:** logic, backward-compatibility, filtering
**Scope:** packages/core/src/**/*.ts, !**/*.test.*, !**/*.spec.*

When introducing status fields to legacy datasets, use negative filters (e.g., `!== 'archived'`) instead of positive ones to ensure records lacking the field remain active by default.
