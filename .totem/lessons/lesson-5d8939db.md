## Lesson — Standard shell string manipulation is insufficient

**Tags:** bash, json, automation

Standard shell string manipulation is insufficient for robust JSON parsing in hooks; using `jq` ensures reliable data extraction from structured configuration files.
