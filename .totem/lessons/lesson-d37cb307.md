## Lesson — Material updates to system prompts should include explicit

**Tags:** prompt-engineering, observability, agents

Material updates to system prompts should include explicit version identifiers to help correlate changes in agent behavior with specific prompt iterations. This allows developers to debug regressions in output quality or adherence to strict execution gates that otherwise appear as random model fluctuations.
