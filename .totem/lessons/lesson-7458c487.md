## Lesson — Isolate home directories in ledger tests

**Tags:** testing, config, dx
**Scope:** packages/**/*.test.ts

Config resolvers that fall back to global profiles can silently pollute the developer's live ledger during test runs. Always inject and mock a temporary home directory throughout the resolver chain in tests.
