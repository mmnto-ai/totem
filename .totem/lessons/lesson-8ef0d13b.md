## Lesson — Place invariants and test requirements directly

**Tags:** prompt-engineering, ai-agents, context-window

Place invariants and test requirements directly within individual implementation tasks rather than at the top of a specification. This ensures critical constraints remain in the AI agent's active context window at the exact moment of execution.
