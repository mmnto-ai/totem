## Lesson — Use delimiters in multi-field cache keys

**Tags:** caching, security
**Scope:** packages/cli/src/utils.ts

Concatenating multiple fields for a hash key without delimiters (e.g., null bytes) allows boundary collisions where different inputs produce identical keys.
