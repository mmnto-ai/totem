## Lesson — Include path context in walkDir errors

**Tags:** testing, dx
**Scope:** packages/pack-agent-security/test/**/*.ts

Filesystem traversal helpers should never swallow errors or use empty catch blocks. Rethrowing errors with the specific directory or file path ensures test failures are deterministic and easy to debug.
