## Lesson — Regex for semantic directive detection

**Tags:** regex, markdown, llm
**Scope:** packages/cli/src/commands/**/*.ts, !**/*.test.*, !**/*.spec.*

Use regex patterns for bullet points and numbered lists as a reliable proxy for counting semantic directives in Markdown-based instruction files.
