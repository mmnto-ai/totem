## Lesson — Avoid forcing case normalization on file path filters

**Tags:** sql, lancedb, filesystem

Avoid forcing case normalization on file path filters to remain consistent with case-sensitive filesystems like Linux and macOS. This prevents query performance overhead and ensures compatibility with SQL dialects, such as LanceDB/DataFusion, that may not support case-insensitive operators.
