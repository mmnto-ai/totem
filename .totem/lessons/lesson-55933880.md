## Lesson — Use branch diffs for pre-push hooks

**Tags:** git, cli, hooks
**Scope:** packages/cli/**/*.ts, !**/*.test.*

Standard git diff commands often return only uncommitted changes, which are empty at push time; use `git diff <base>...HEAD` to capture the actual content being pushed.
