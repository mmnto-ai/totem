## Lesson — Enforce structural compilation over context escapes

**Tags:** llm, compiler, ast-grep
**Scope:** packages/cli/src/commands/compile-templates.ts

Compilation must succeed if structural combinators can express the guard, preventing the LLM from using 'context-required' as an escape hatch for complex patterns.
