## Lesson — Tests should invoke the actual command handler rather

**Tags:** testing, validation

Tests should invoke the actual command handler rather than mirroring validation logic to ensure regressions in the real validation path are caught.
