## Lesson — Use sign-sensitive glob set comparison

**Tags:** glob, logic
**Scope:** packages/core/src/lesson-pattern.ts

When comparing sets of globs, use logic that is order-insensitive and duplicate-insensitive but remains sensitive to negation prefixes to detect true divergence.
