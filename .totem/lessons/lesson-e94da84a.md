## Lesson — Normalize metadata before exact matching

**Tags:** normalization, dx
**Scope:** packages/core/src/**/*.ts, !**/*.test.*, !**/*.spec.*

Lowercase and collapse whitespace in metadata fields like headings to catch duplicates that differ only by casing or formatting.
