## Lesson — Avoid labeling a tool suite as "deterministic" or "fast"

**Tags:** documentation, terminology, architecture

Avoid labeling a tool suite as "deterministic" or "fast" if it contains AI-powered components. Property descriptions should be attributed to specific commands (e.g., `totem lint`) to ensure users understand which parts of the system provide hard guarantees versus AI reasoning.
