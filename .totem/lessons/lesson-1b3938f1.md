## Lesson — Use xargs instead of 'tr' for joining multi-line outputs

**Tags:** shell, posix, portability

Use xargs instead of 'tr' for joining multi-line outputs into space-separated strings to ensure POSIX compliance and avoid inconsistent newline escaping behavior.
