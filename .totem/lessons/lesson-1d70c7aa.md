## Lesson — False positives are often caused by overlapping

**Tags:** debugging, linting, rule-generation

False positives are often caused by overlapping or duplicate rule definitions in the generated output rather than bugs in the matching logic. Isolating the specific rule IDs in compiled files helps distinguish between engine errors and data generation flaws.
