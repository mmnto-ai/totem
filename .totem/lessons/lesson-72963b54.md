## Lesson — Reformatting files like compiled-rules.json invalidates

**Tags:** ci, dx, formatting

Reformatting files like compiled-rules.json invalidates checksums stored in manifests, leading to CI attestation failures. Always exclude these files from Prettier to maintain build integrity.
