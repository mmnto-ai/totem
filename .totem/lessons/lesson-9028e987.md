## Lesson — Unified-diff prefixes (like '+', '-', or ' ') must be

**Tags:** compiler, regex, git

Unified-diff prefixes (like '+', '-', or ' ') must be stripped from source lines before synthesizing regex patterns to ensure the resulting rules match actual source code rather than patch hunks.
