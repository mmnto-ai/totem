## Lesson — Processing code suppressions after a rule match allows

**Tags:** telemetry, rule-engine

Processing code suppressions after a rule match allows the engine to emit events for ignored violations. Filtering out suppressed lines before running queries silently drops metrics and diagnostic data needed for suppression tracking.
