## Lesson — Skip test timeouts for pure data suites

**Tags:** testing, performance, timeouts
**Scope:** packages/cli/src/commands/**/*.test.ts

General rules requiring explicit test timeouts should not be blindly applied to pure data test suites that perform no I/O. Forcing timeouts on fast, synchronous tests adds boilerplate without providing any real safeguard.
