## Lesson — Avoid splitting error messages on periods

**Tags:** error-handling, dx, regex
**Scope:** packages/core/src/**/*.ts, !**/*.test.*, !**/*.spec.*

Splitting error strings on periods can truncate critical debugging context when the error contains code snippets starting with dots (e.g., .method()). Use newline-based splitting or regex to preserve the full diagnostic line.
