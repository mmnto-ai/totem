## Lesson — Hardcoded default values for properties like rule

**Tags:** style, curated
**Pattern:** \b(category|ruleCategory)\s*:\s*['"][^'"]+['"]
**Engine:** regex
**Scope:** **/\*.ts, !**/\*.test.ts
**Severity:** warning

Define rule categories as shared constants in the core schema definition instead of hardcoded strings to prevent logic drift between reporting and CLI layers.
