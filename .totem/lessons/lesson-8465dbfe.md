## Lesson — Applying '// totem-ignore' within code-like string fixtures

**Tags:** testing, linting

Applying '// totem-ignore' within code-like string fixtures prevents the compiler from aggressively flagging intentional test data as errors or linting violations.
