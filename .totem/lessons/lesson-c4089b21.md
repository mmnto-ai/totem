## Lesson — Avoid duplicating interface definitions across modules even

**Tags:** typescript, dx

Avoid duplicating interface definitions across modules even if they are structurally identical; use a single source of truth to prevent silent type contract failures during refactoring.
