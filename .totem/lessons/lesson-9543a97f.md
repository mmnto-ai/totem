## Lesson — Anchor suppression comments to target lines

**Tags:** linting, dx, totem
**Scope:** packages/**/*.ts, !**/*.test.*

The `// totem-context:` suppression directive must be placed on the line immediately preceding the flagged expression to be recognized by the deterministic lint engine.
