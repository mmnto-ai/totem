## Lesson — Initialize manifests with schema wrappers

**Tags:** json, architecture, schema
**Scope:** packages/pack-agent-security/compiled-rules.json

Initialize empty rule manifests as '{ "version": 1, "rules": [] }' rather than bare arrays to ensure forward compatibility with schema-aware loaders.
