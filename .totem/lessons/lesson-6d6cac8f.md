## Lesson — Explicitly exclude test files (e.g., !*/.test.ts)

**Tags:** linting, dx

Explicitly exclude test files (e.g., `!**/*.test.ts`) from linting rule scopes to prevent false positives in code where specific patterns are intentional.
