## Lesson — Distinguish lint format from json flags

**Tags:** cli, dx
**Scope:** README.md

The --format flag is exclusive to the lint command, while other CLI commands use a standard --json flag for scripting output.
