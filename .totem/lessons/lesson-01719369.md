## Lesson — Export types in WASM shims

**Tags:** typescript, wasm, ast-grep
**Scope:** packages/core/src/ast-grep-wasm-shim.ts

When creating a WASM-based compatibility shim for a native API, ensure all used types (like SgRoot and SgNode) are exported to prevent downstream type-checking failures.
