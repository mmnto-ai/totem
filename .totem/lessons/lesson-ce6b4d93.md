## Lesson — Every stage of a multi-gate security hook must explicitly

**Tags:** devops, security, ux

Every stage of a multi-gate security hook must explicitly check for its own prerequisites to provide clear 'how-to-fix' guidance. Relying on silent failures in downstream commands creates an inconsistent and confusing user experience.
