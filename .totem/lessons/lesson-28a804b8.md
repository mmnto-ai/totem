## Lesson — Passing a trigger context (e.g., via environment variables)

**Tags:** cli, observability

Passing a trigger context (e.g., via environment variables) when a hook calls a CLI allows the tool to differentiate between manual and automated runs for logging.
