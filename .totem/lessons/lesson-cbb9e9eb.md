## Lesson — Test regex escaping with character classes

**Tags:** testing, regex
**Scope:** packages/core/src/**/*.ts, !**/*.test.*, !**/*.spec.*

Validate regex escaping by interpolating the output into a character class in a test to ensure metacharacters do not trigger range behavior.
