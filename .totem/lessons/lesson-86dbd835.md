## Lesson — Use totem-context for justified rule suppressions

**Tags:** dx, totem
**Scope:** packages/pack-agent-security/test/**/*.ts, !**/*.test.*, !**/*.spec.*

The `totem-context:` directive is the only recognized way to provide inline justifications for rule violations; `totem-ignore:` is unsupported and will be ignored by the pipeline.
