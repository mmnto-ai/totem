## Lesson — Log optional component failures at the warn level rather

**Tags:** logging, observability, cli

Log optional component failures at the `warn` level rather than using dimmed or silent logs to ensure that environmental misconfigurations are discoverable. Even when a system is designed for graceful degradation, failures should be visible enough to aid debugging.
