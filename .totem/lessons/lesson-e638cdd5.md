## Lesson — Redirecting hook command output to /dev/null prevents users

**Tags:** git, dx, ux

Redirecting hook command output to /dev/null prevents users from diagnosing why a push was blocked; hooks should instead provide specific diagnostic hints or commands.
