## Lesson — Implement cleanup for database connections

**Tags:** database, resources
**Scope:** packages/cli/src/commands/**/*.ts, !**/*.test.*, !**/*.spec.*

Even if a store currently lacks a close method, wrapping connections in try-finally blocks with feature-detection for cleanup methods prevents resource exhaustion in long-running processes.
