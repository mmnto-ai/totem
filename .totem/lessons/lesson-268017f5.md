## Lesson — Use pnpm exec for workspace binaries in monorepos

**Tags:** style, curated
**Pattern:** \bpnpm\s+bin\b
**Engine:** regex
**Scope:** *.sh, *.bash, *.yml, *.yaml, package.json
**Severity:** warning

Use pnpm exec for workspace binaries in monorepos.
