## Lesson — Avoid falling back to the raw input string when a split

**Tags:** typescript, parsing, clean-code

Avoid falling back to the raw input string when a split results in a single item; using the processed array ensures that normalization steps like trimming and filtering are consistently applied.
