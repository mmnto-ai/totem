## Lesson — Use dynamic import() within CLI command handlers instead

**Tags:** cli, performance

Use dynamic `import()` within CLI command handlers instead of top-level static imports to minimize startup latency and improve responsiveness.
