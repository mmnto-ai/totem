## Lesson — Enforce pack compatibility via engines contract

**Tags:** npm, versioning, governance
**Scope:** docs/wiki/pack-ecosystem.md

Using the `engines` field in `package.json` allows rule packs to define strict version requirements for the host CLI. This ensures the ecosystem can validate compatibility before loading third-party packs, preventing runtime failures.
