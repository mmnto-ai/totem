## Lesson — Returning a result object with a specific rejection reason

**Tags:** validation, dx

Returning a result object with a specific rejection reason instead of a simple null allows callers to distinguish between an absent pattern and a malformed one.
