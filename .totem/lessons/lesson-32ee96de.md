## Lesson — Provide actionable hints in configuration errors

**Tags:** cli, dx, ux
**Scope:** packages/cli/src/**/*.ts

CLI errors for missing dependencies should include specific hints, such as sibling clone commands or environment variable references, to reduce developer friction.
