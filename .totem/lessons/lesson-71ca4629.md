## Lesson — Validate paths using path.resolve and path.relative

**Tags:** security, node.js, agents

Validate paths using path.resolve and path.relative to ensure agent-driven file operations stay within the workspace. This prevents directory traversal attacks where an agent might access sensitive system files.
