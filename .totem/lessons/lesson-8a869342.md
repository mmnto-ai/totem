## Lesson — Enforce exclusivity for manifest refresh

**Tags:** cli, logic
**Scope:** packages/cli/src/commands/compile.ts

Enforce strict exclusivity between --refresh-manifest and --force because refreshing assumes the current state is authoritative while forcing a recompile explicitly invalidates it.
