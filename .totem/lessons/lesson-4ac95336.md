## Lesson — When deduplicating multiple findings into a single row,

**Tags:** linting, logic

When deduplicating multiple findings into a single row, the group must inherit the highest severity (error) to ensure critical issues are not masked by warnings.
