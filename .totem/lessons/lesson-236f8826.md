## Lesson — Use overloads for polymorphic field routing

**Tags:** typescript, type-safety
**Scope:** packages/core/src/compiler.ts

Applying TypeScript overloads to functions handling multiple engine types prevents silent object-to-string coercion when passing objects to engines that only support strings.
