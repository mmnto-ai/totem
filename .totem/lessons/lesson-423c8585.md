## Lesson — Escape Windows paths in generated configs

**Tags:** cli, windows, node
**Scope:** packages/cli/src/commands/init.ts

When programmatically generating configuration files containing absolute paths, ensure backslashes are properly escaped for Windows compatibility. Unescaped backslashes in `.ts` or `.json` files are interpreted as escape sequences, resulting in corrupted paths at runtime.
