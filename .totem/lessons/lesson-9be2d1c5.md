## Lesson — When a required external dependency or model is missing,

**Tags:** error-handling, dx, dependencies

When a required external dependency or model is missing, error messages should provide the specific command needed to resolve the issue (e.g., 'ollama pull ...'). This significantly reduces user friction and the need for manual troubleshooting.
