## Lesson — Use Jaccard similarity for deterministic coverage

**Tags:** algorithms, heuristics
**Scope:** packages/core/src/recurrence-stats.ts

A Jaccard similarity threshold (e.g., 0.6) on tokenized messages provides a deterministic, low-dependency heuristic for mapping findings to existing rules without requiring LLM embeddings.
