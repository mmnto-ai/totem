## Lesson — Preserve additive fields in union success paths

**Tags:** api-design, typescript, schema
**Scope:** packages/mcp/**/*.ts

When evolving a schema into a discriminated union, keeping original fields in the success branch maintains backward compatibility for consumers who read those fields directly without checking the discriminator.
