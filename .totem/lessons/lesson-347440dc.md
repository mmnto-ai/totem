## Lesson — When structured output flags are active, redirect all

**Tags:** cli, ux

When structured output flags are active, redirect all human-readable logs to `stderr` to prevent corruption of the `stdout` stream intended for machine parsing.
