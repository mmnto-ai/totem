## Lesson — Always use the !**/*.ext pattern for recursive exclusions

**Tags:** glob, testing

Always use the !**/*.ext pattern for recursive exclusions to ensure consistency and prevent shallow matching that misses nested test files.
