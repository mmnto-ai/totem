## Lesson — When upserting a managed PR comment identified by an HTML

**Tags:** github-api, automation

When upserting a managed PR comment identified by an HTML marker, selecting the highest (newest) ID prevents the logic from updating stale or duplicate markers.
