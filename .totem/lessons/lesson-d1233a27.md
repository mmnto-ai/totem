## Lesson — Choosing simpler patterns that align with enforced codebase

**Tags:** regex, maintainability, linting

Choosing simpler patterns that align with enforced codebase styles is better than complex regex that covers unused syntax. This reduces the technical debt of maintenance without sacrificing practical accuracy in specific environments.
