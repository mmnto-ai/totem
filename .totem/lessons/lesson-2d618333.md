## Lesson — Hardcoding the CLI name or description in custom help

**Tags:** cli, commander, dx

Hardcoding the CLI name or description in custom help formatters leads to divergence from the actual program configuration. Use cmd.name() and cmd.description() to ensure help output stays in sync with the root command.
