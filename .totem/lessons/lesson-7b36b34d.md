## Lesson — Markdown field extractors should allow zero-length matches

**Tags:** regex, parsing, markdown

Markdown field extractors should allow zero-length matches to ensure that present but empty headings are explicitly detected and validated rather than ignored.
