## Lesson — Dynamic imports used for CLI startup performance should be

**Tags:** architecture, performance

Dynamic imports used for CLI startup performance should be restricted to command entry points to maintain a clean dependency graph and pass security scans in utility layers.
