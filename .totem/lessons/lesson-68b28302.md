## Lesson — Explicitly verify that glob entries are strings

**Tags:** typescript, validation, globs

Explicitly verify that glob entries are strings before calling methods like .startsWith to prevent runtime crashes when processing arrays that might contain non-string types.
