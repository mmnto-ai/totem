## Lesson — Prefer resolvers over git submodules

**Tags:** git, architecture, dx

Replacing submodules with a multi-layer resolver (env > config > sibling) eliminates gitlink drift churn and simplifies fresh checkout ceremonies.
