## Lesson — Detect incomplete bootstrap during init

**Tags:** cli, ux
**Scope:** packages/cli/src/commands/init.ts

The `init` command should verify the existence of all critical artifacts, such as compiled rules, before reporting success. Failing to detect an incomplete bootstrap leaves the user with a broken installation that lacks a clear path to repair.
