## Lesson — Background git hooks require both application-level flags

**Tags:** git, shell, ux

Background git hooks require both application-level flags and shell-level stderr redirection to prevent Node.js stack traces from polluting the terminal during git operations.
