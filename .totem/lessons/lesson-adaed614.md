## Lesson — Use broad matching or normalization when scanning

**Tags:** git-hooks, automation

Use broad matching or normalization when scanning for existing hooks to prevent duplicate injections when the command name or invocation style changes.
