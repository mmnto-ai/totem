## Lesson — Repository conventions require the 'Totem Error' tag

**Tags:** cli, logging

Repository conventions require the 'Totem Error' tag for all `log.error` calls to ensure consistent diagnostic parsing and filtering across the CLI.
