## Lesson — Include trailing glob when overriding Turbo inputs

**Tags:** turbo, monorepo, ci
**Scope:** turbo.json

In Turbo v2, defining an 'inputs' array for a task overrides the default package-level file hashing entirely. You must include a trailing '**' to ensure that changes to local package source code still invalidate the cache.
