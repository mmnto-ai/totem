## Lesson — Using empty catch blocks is acceptable for non-critical,

**Tags:** architecture, error-handling

Using empty catch blocks is acceptable for non-critical, best-effort features like hint extraction to prevent surfacing confusing I/O warnings that do not impact core functionality.
