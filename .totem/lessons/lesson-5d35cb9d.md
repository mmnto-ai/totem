## Lesson — Manually prune retired git submodule directories

**Tags:** git, dx

Git does not automatically delete formerly-tracked submodule directories or their internal metadata from the filesystem after they are removed from .gitmodules.
