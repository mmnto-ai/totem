## Lesson — When testing CLI logic that modifies process.exitCode,

**Tags:** testing, node

When testing CLI logic that modifies process.exitCode, the exitCode must be reset in a global teardown to prevent state leakage between independent test cases.
