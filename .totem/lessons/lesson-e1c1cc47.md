## Lesson — Include both quote styles in AST patterns

**Tags:** ast-grep, dx
**Scope:** packages/pack-agent-security/compiled-rules.json

AST-grep patterns matching specific string literals (e.g., encoding types) must explicitly define both single and double quote variants to ensure robust detection.
