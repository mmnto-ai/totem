## Lesson — Restricting attestation workflows to specific code paths

**Tags:** github-actions, ci-cd

Restricting attestation workflows to specific code paths prevents false CI failures on documentation-only PRs that do not produce the expected build artifacts.
