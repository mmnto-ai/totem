## Lesson — Using 'git merge-base --is-ancestor' allows validation

**Tags:** git, dx, performance

Using 'git merge-base --is-ancestor' allows validation gates to skip re-execution if only non-target files have changed since the last successful check.
