## Lesson — LLMs are unreliable for calculating precise line proximity;

**Tags:** llm, typescript, deduplication

LLMs are unreliable for calculating precise line proximity; implementing deduplication in a deterministic language like TypeScript ensures consistent results for overlapping comments.
