## Lesson — While manual edits to compiled artifacts are generally

**Tags:** architecture, linting, workflow

While manual edits to compiled artifacts are generally discouraged, manually promoting a rule from 'warning' to 'error' is the project's standard way to enforce architectural gates.
