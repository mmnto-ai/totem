## Lesson — Claude Code hooks require a specific object structure

**Tags:** claude-code, hooks, configuration

Claude Code hooks require a specific object structure containing a `matcher` field within the hook definition to execute correctly. The `/doctor` command is the standard way to verify that these configurations meet the tool's expected internal schema.
