## Lesson — Validate all preconditions before filesystem writes

**Tags:** fs, atomicity, integrity
**Scope:** packages/cli/src/commands/spine-authored-materialize.ts

Performing integrity checks after initial file writes can leave partial or corrupt artifacts on disk if a later check fails. All validations must be completed before the first write call to maintain a 'nothing-written-on-failure' guarantee.
