## Lesson — Explicitly capture stderr when a tool probes multiple

**Tags:** cli, dx, architecture

Explicitly capture stderr when a tool probes multiple repositories for a resource to prevent 'not found' or GraphQL errors from polluting the terminal during successful execution.
