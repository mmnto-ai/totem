## Lesson — When detecting markers in user-authored files, use

**Tags:** style, curated
**Pattern:** \.(includes|indexOf|match)\(\s*['"]marker['"]
**Engine:** regex
**Scope:** **/*.ts, **/*.js, **/*.tsx, **/*.jsx, !**/*.test.ts
**Severity:** warning

When detecting markers in user-authored files, use.
