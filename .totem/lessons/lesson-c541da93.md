## Lesson — Reference rule IDs over hard-coded metrics

**Tags:** documentation, maintenance
**Scope:** docs/wiki/*.md

Use rule identifiers (e.g., FR-C01) instead of hard-coded numeric thresholds in documentation to maintain accuracy when configuration values are updated.
