## Lesson — Use a Personal Access Token (PAT) for automated PR creation

**Tags:** github-actions, security

Use a Personal Access Token (PAT) for automated PR creation if the organization restricts the default GITHUB_TOKEN from performing those actions.
