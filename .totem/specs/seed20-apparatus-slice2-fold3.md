# seed-20 apparatus slice 2 — falsification-leg fold 3 (narrow third read on `e3f4ceae..a05e6948`, deposit 2026-08-29 ~22:20Z)

H1, H3, H4, H5, H10 verified FOLDED (H1 recomputed both ways against the scorer's coded join; H3's pathspec tested empirically against the collapsed-untracked-directory case; H5's two digests recomputed independently; H10's true/true/false reproduced on the shipped parser). H2 PARTIAL. One BLOCKING introduced by fold 2's H6, three MATERIAL on the re-pin path, five MINOR. Dispositions below; implemented by one TS leg (`spikes/spine-adopt/src/**`), verified at source by the owner, then the seam. No further falsification pass is armed: the items are mechanical closures of findings already deposited, and the seam re-proves them.

## Adopted

- **J1 (BLOCKING — H6 undone at the seam).** `checks[]` is NOT published inside `manifest.json`: the scorer re-derives `runManifestSha256` deleting only `bundles`, `bundlesSha256`, `runManifestSha256` (charter § 5: "any manifest sha256 mismatch" refuses), so a `checks` key inside the manifest refuses every run of record. The manifest stage publishes its rows as a sibling artifact `artifacts/manifest-checks.json` (`{ generatedBy, contract, recordSet, checks[] }`, stamped by `writeArtifact` like every other artifact; written after the manifest so it carries the run's `runManifestSha256`). `computeRunManifestSha256` deletes exactly the three keys the scorer deletes — nothing else; the unfalsifiable `checks`-survival guard in `fillManifestBundles` goes with it. `controls.json` K6 keeps summarising the byte-identity rows (refusing), so the machine-readable path is unchanged; the sibling artifact is where the manifest stage's other rows live.
- **J2 (MATERIAL — arm B bound to this run).** `manifest.mts` reads `artifacts/k3-control/manifest.json` beside the arm-B bytes and treats arm B as PRESENT only when that manifest's `recordSet === 'control'` AND its `runCommit` equals this run's `HEAD`; otherwise arm B is absent (`repinned: null`, `repinnedFrom: null`) and a disclosing row names the stale build's `runCommit` and record set. The same binding is applied by `controls.mts` K3 arm B.
- **J3 (MATERIAL — a re-pin is DISCLOSED, never refused, on the apparatus too).** `controls.mts` K3: arm B's bytes equal the captures ⇒ `armB: 'stands'`; differ AND `manifest.k3Capture.repinned === true` with the flat keys present ⇒ `armB: 'repinned'` (the row records the re-pinned digests and `repinnedFrom`; `ok` stays `true` for the K3 row on that account); differ but the manifest says `repinned !== true` ⇒ `ok: false` (the two artifacts disagree — an apparatus fault). Arm A compares the seed chain against the RE-PINNED target (`artifacts/k3-control/chains/<pkg>.json`) when `repinned === true`, against the capture otherwise — the scorer reads `repinnedFrom` the same way.
- **J4 (MATERIAL — `null` is not scoreable).** On `seed20`, `k3Capture.repinned === null` (arm B absent or unbound) is a FAIL check — § 6's order is "the control-only build first" — unless `SPIKE_ALLOW_DIRTY_TREE=1` (the dev-seam flag; the run of record does not set it). The header prints the outcome.
- **J5 (MINOR — K8's fallback arm).** No `[Totem Error] <file>: <keyPath> — <detail>` inner form ⇒ `namesTheField: false` (a rejection that is not the § Design 5 PARSER naming the field — e.g. a `compileRuleRecord` reason — is not K8's evidence); the row's `keyPath` marker says so.
- **J6 (MINOR).** `record-sets.mts`'s docstring states the actual predicate `facts.mts` uses (`intake.accepted.find(r => requiresScopeOf(r.compiled.rule) === 'file')`, no `control` filter) and why the two coincide on every set today.

## Disclosed (carried to the PR-open mail; no code change)

- **`workingTreeClean` attests the SOURCE tree** (H3). The charter's K6 words are "`git status --porcelain` was empty at production"; on a proper run of record the value is `true` under both readings (the only dirt is the run's own artifact output, excluded and recorded as `artifactTreeEntries`), so the scorer's refusal never fires — but the charter's wording should say "the source tree; the artifact trees the run writes are recorded beside". A v1.1 wording note.
- `artifactTreeEntries` sits inside the `runManifestSha256` preimage (like `generatedAt`); the run identity therefore moves with leftovers under the artifact trees — stated, not changed.
- `wazero-report.json`'s `runManifestSha256` is written from the same variable the pinned pairs writer receives; only the pairs half has a test.

## Seam the runner re-proves

Unchanged from fold 2 (control-only first; no `SPIKE_ALLOW_DIRTY_TREE`), plus: the seed manifest binds arm B to `HEAD`; `artifacts/manifest-checks.json` present with 0 failing rows; the manifest's key set carries no `checks`; the specimens regeneration classified.
