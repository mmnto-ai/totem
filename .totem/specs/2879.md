### Problem Statement

`totem gate check --event merge-ready` predicate 1 (the status-check floor) reads the PR's GraphQL `statusCheckRollup.contexts` and classifies every `CheckRun` node whose conclusion is not a success as `failing`. The rollup lists EVERY check run on the head, including the run a workflow's `cancel-in-progress` concurrency group cancelled because a later run of the same workflow superseded it. A head with two D1 runs, one `CANCELLED` (superseded) and one `SUCCESS`, read "1 of 16 status checks are failing" at both tiers while `gh pr checks` and GitHub's merge box showed every check green (mmnto-ai/totem#2877 at `46ef1840` and `7d22c1fb`, 2026-09-16). The floor denied a mergeable PR.

### Ruled design (2026-09-18, the seat's call as implementer; the issue's proposal amended by a measurement; one falsification leg folded)

**Judge a check NAME by its latest run; the latest run is the greatest `databaseId`.**

- The `CheckRun` fragment selects `databaseId` beside `name status conclusion`. GitHub's check-run id is one increasing sequence, so among same-named runs on one head the greatest id IS the latest run. The invariant is creation order WITHIN a name (ids are minted at queue time; across different names id order and start order differ), which is exactly what the collapse needs. The leg re-derived it on 31 live same-named groups (60 `main` commits plus the PR heads): 31 of 31 agree. No other field is added: `startedAt` is nullable on a queued run and would need parsing; `checkSuite.app.slug` would widen the fragment for a collision the cohort does not have (see the residual below).
- **The rollup's own order is NOT an ordinal.** The issue proposed "the rollup's own order, which is newest-last" as the fallback. A live read on 2026-09-18 of mmnto-ai/totem#2877's head `46ef1840` listed the LATER D1 run (started 03:44:23Z, id 104657003920) BEFORE the earlier one (03:42:57Z, id 104656726470); mmnto-ai/totem#2871's head lists its pair the other way round. Neither "last listed wins" nor "first listed wins" survives: each would misjudge one of the two real heads, and two fixtures list the greater id in opposite positions so both mutants die.
- **A rerun mints a NEW, greater id, and the rollup keeps only the latest attempt.** The originating incident reconstructs from the Actions attempts API: attempt 1 of run 35052802132 was check run `104656626085` (CANCELLED, started 03:42:25Z) and the superseding run was `104656726470` (SUCCESS); the rerun replaced the cancelled run with `104657003920`. The greatest id at bug time was the SUCCESS, so this read cures the head that surfaced the issue; the spec's ordering specimen above is the post-rerun pair, quoted for the ordering point only.
- **A duplicate group with an unreadable id is an unreadable check state.** If a name ran more than once and any of its runs carries no non-negative-integer `databaseId` (the schema types it nullable, so the arm is reachable), the read returns the UNEVALUABLE class (deny under strict, warn under pilot) naming the check, never a guess at which run is latest. A name that ran once needs no id and is judged on its own conclusion (pinned).
- **Any earlier run is superseded, cancelled or failed.** The collapse judges on the id alone; an earlier `FAILURE` followed by a later `SUCCESS` of the same name passes predicate 1 (a live shape on `main`, where a five-run name begins with a failure). This is what `gh pr checks` shows; the rollup's own `state` (FAILURE on such a head) is read only on the zero-checks arm.
- **`StatusContext` nodes pass through untouched.** Legacy statuses already carry one state per context. A `CheckRun` whose name did not read (the `(unnamed check)` placeholder; `name` is NON_NULL in the schema, so only a malformed payload reaches it) passes through too, so two malformed nodes are never read as one check that ran twice.
- **Counts.** `checks.total` counts names after the collapse, the way `gh pr checks` and the merge box count; `checks.superseded` (new, required on the exported type) counts the replaced RUNS, not names (pinned by a three-run group). The materialisation check (`totalCount` must equal the nodes read) stays on the RAW nodes: the rollup accounts for what it listed, and the collapse is a read of those nodes, run after that check and after the LAST page (pinned by a pair split across two pages).
- **Disclosure.** One stderr line PER collapsed name, sanitised and never sliced: the name, its run count, the judging run's id and kind, and the number of superseded runs. A single line under the 160-character evidence bound lost its tail at three names (the leg found a `main` head with five), and a disclosure that trails off is not one.
- **The negative that must hold.** A lone `CANCELLED` run with no later run of its name stays `failing`, and a name whose LATEST run is cancelled stays `failing` however many earlier runs of it succeeded.
- **Semver: minor.** `MergeReadyProvenanceDetail.checks` gains a required field and `total` changes meaning; no consumer outside core reads the shape (the CLI types `detail` as an opaque record), but a TS consumer constructing the type breaks at compile time. The fixed changeset group moves every `@mmnto/*` package with it.

### Files touched

- `packages/core/src/merge-ready.ts`: the fragment (`databaseId`), `CheckEntry` (`typename`, `runId`), `readChecks` (reads the id), `judgeLatestRuns` (the collapse, returning a `SupersededRun` record per name; called in `readPullRequest` after the count check), `summarizeChecks` (judged entries plus the superseded run count), `oneLine` (the unsliced sanitiser `bounded` now builds on), the per-name notices, `firstFailure` (names from the judged list), `MergeReadyProvenanceDetail.checks.superseded`.
- `packages/core/src/merge-ready.test.ts`: eight fixture-backed tests plus a query-shape pin; the split count moves 54/59 → 61/66; every `checks` `toEqual` gains `superseded: 0`; the 2871 capture assertion moves to 16 names + 1 superseded.
- `packages/core/src/gate-fixtures/merge-ready/`: every synthetic `CheckRun` node stamped with an id (5001, 5002, … in node order per fixture; `synthesizedAt` unchanged); the four PR captures re-captured with the new query by running the real evaluator with a runner that spawns `gh` (argv) and tees each page; seven new synthetic fixtures (`synthetic-check-superseded-cancelled`, `-cancelled-after-success`, `-single-cancelled`, `-duplicate-id-missing`, `-three-runs-two-names`, `-single-null-id`, `-duplicate-across-pages`); the README preface, capture instants, every sha256 and the query sha rewritten from the files on disk after `prettier`.
- `.changeset/merge-ready-latest-run-2879.md`: minor on `@mmnto/totem`.

### Residual, disclosed

Two check runs that share a name on one head — two GitHub Apps, or two jobs under one app (Actions names a check run after the JOB, so keying on the app slug alone would not separate two workflows with the same job name) — collapse to the run with the greater id, so the later one judges the name. Not reachable in this repo today: every job name across the thirteen workflows is distinct, and the only second app on a live head (`github-advanced-security`, posting `totem-lint`) differs from Actions' `Totem Lint` in case, so the exact-string key keeps them apart. If a same-name pair ever appears, key the group on the run's identity then; the collapse function is the one place to change.

### Verification

- The seven new fixtures: the superseded cancel allows with `superseded: 1` and one notice; the later cancel denies naming the check; the lone cancel denies with no notice; the duplicate with a null id is unevaluable at both tiers naming the check and `databaseId`; the three-run group allows with `superseded: 3` and two whole-name notices; the lone null-id run allows with no notice; the cross-page pair allows with the second call carrying the checks cursor.
- Every pre-existing assertion holds after the reshape (the reshape added a field and changed no verdict). The materialised node counts of the four captures are unchanged by the re-capture (6, 3, 16, 17); the one capture assertion that moved is mmnto-ai/totem#2871's, to 16 names + 1 superseded, because that head really carries a duplicated name.
- The README receipts test: every sha256, the capture instants and the query sha re-derived from disk.
- `pnpm -r build && pnpm -r test`, ESLint, `prettier --check`, full-branch `totem lint`; one falsification leg folded (0 blocking, 4 material, 7 minor; convergence named by the leg), re-armed once on the notice's emission change.
