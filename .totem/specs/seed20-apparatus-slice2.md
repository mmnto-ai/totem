# R14 seed-20 compilation-target trial — apparatus slice 2 (G2 generator · K5 control record · K8 · K6 · K3 control-only build · T5 sweep · `controls.json` · wazero grammar · K4/K7 homes)

> **Ritual note.** `totem spec seed20-apparatus-slice2` ran (run artifact `.totem/artifacts/runs/f460bf35fdb2….json`, `caller = spec`, 2026-08-29T19:31Z) and drafted a "Zod configuration sweep engine" — the fourth slug-topic hallucination of this class (mmnto-ai/totem#2687 lineage). Replaced by hand; nothing below is LLM-authored. Every `file:line` anchor is at `044e114b` and was read by the owner or by a read-only explore leg whose report the owner re-verified at the load-bearing sites.

**Charter:** mmnto-ai/totem-strategy#1154 v1 @ `1c079e2a` (merged 2026-08-29T08:31Z), floor RULED (B) on mmnto-ai/totem-strategy#1138 (comment `5461328514`); **the slice word** — operator 2026-08-29 (relayed in strategy-claude's 19:23Z dispatch, adopted in the owner's 19:24Z + 19:28Z replies, which fix the names below); charter v1.1 (a versioned erratum, apparatus + control wording only) binds to these names from strategy's lane. **Lane:** totem-claude = apparatus-runner. **This slice is APPARATUS, never scoring: it emits rows, artifacts and control outcomes; it decides no verdict.** Predecessors: `.totem/specs/seed20-apparatus.md` (G1–G10 + K4) and `seed20-apparatus-round1.md` (T*/C1–C4) — their constraints stand; `T*`/`C\*` tokens below continue those numberings.

## Hard constraints (each one a refusal condition on the PR)

1. **No run of record. Nothing scored. No pin mailed.** Local `seed20` runs to exercise the apparatus are required by § Verification; **no seed-run artifacts are committed** — `spikes/spine-adopt/artifacts/**` and `wazero-probe/artifacts/**` stay at the 7-specimen baseline plus the additive/identity deltas § Verification enumerates. After any local seed run: `git checkout -- spikes/spine-adopt/artifacts spikes/spine-adopt/wazero-probe/artifacts` and remove untracked outputs (incl. any `artifacts/<subdir>/`) before staging. The specimens baseline is regenerated LAST, and its byte delta is listed in the PR body key by key.
2. Branch `r14/seed20-apparatus-slice2` from main `044e114b` (worktree `D:/Dev/worktrees/totem-totem-claude-seed20-slice2`). The pin is cut from post-merge main; the run of record happens there, on the operator's word.
3. **The EMITTED POLICY stays byte-stable:** `src/lower.mts:286-561 emitPolicy` and its feeders (`:99 factSchemaLine`, `:146 globToRegexSource`, `:248 packageSuffix`, `:77`/`:88` marker tables, `:211 stripRegoStringLiterals`) are not edited for behaviour — the seven committed `artifacts/lowered/**` and `artifacts/chains/**` must reproduce byte-for-byte (INV 2, `src/certify-verify.mts:333-339`), and the K3 capture `artifacts/lowered/r87aff037d7de47a7/policy.rego` (5,448 B, sha256 `528c8d80ce148a404c2a6ff97fcb6df5b889eeeab83228618b5c455932ee48db`) and chain (`artifacts/chains/r87aff037d7de47a7.json`, sha256 `2a4d56caf0b4d18157ecf823374f3bf784b3c857f92faf148aa159f431fe3881`) are unchanged. The § 7 fidelity fix stays deferred; the `fileText` guard hardening (round-1 CR 1) stays deferred.
4. **Frozen inputs, byte-pinned in the tree and re-verified at run time** (`src/verify-records.mts` gains one check per row; sha256 constants live in a new `src/lib/baseline-pins.mts`, the ONLY home for pinned digests):
   - the seed: unchanged, 22 files under `seed/records/` at `2a7135762b6aedc9cd3099ab3f42e029ee34092e` (`record-sets.mts:161-177 seed20Set()` still throws on any file-list drift — **no new file goes under `seed/records/`**);
   - K5 control record `seed/controls/k5/d-requires-file.rule.yaml` — byte-identical to `records/d-requires-file.rule.yaml` (checked by sha equality against the sibling at run time, not by a constant);
   - K8 fixtures `seed/controls/k8/k8-unknown-key.rule.yaml` (sha256 `f6610f9a355d8ce80cbe24eb803421d376f9d350670806ef59cfb2d0492431d4`) and `seed/controls/k8/k8-missing-mandatory.rule.yaml` (sha256 `a0c67c7d4f4762e3d5817115be67ba846b7d7a75eb80d2464459980a48467d84`) — byte-identical to strategy's `operations/310-seed20-target/fixtures/k8/*` (= the greenfield K1/K1b originals; verified by the owner 2026-08-29);
   - K3 capture copies `seed/controls/k3/k3-target.policy.rego` (`528c8d80…48db`) and `seed/controls/k3/k3-target.chain.json` (`2a4d56ca…3881`) — `git show 044e114b:spikes/spine-adopt/artifacts/lowered/r87aff037d7de47a7/policy.rego` / `…chains/r87aff037d7de47a7.json`;
   - the K6 baseline digests at `BASELINE_PIN = 6ca24d42` (§ S4).
5. The 7-specimen baseline stays runnable and reproducible with **no env set** (`activeRecordSet()` default unchanged); `SPIKE_RECORD_SET=seed20` selects the trial set; the new flags are additive.
6. Every comment cites issues as `mmnto-ai/<repo>#N`; never a bare `#N`. No `\uXXXX` escapes authored in source; no shell string-replace over source files.
7. **One falsification leg reads the full diff BEFORE the PR is presented** (the standing pre-merge leg, `doctrine/model-tiering.md` § Review legs) — its typed findings are folded or declined with grounds in the PR body. Local `totem review` lanes do not satisfy this.

## Cross-cutting contract (BOTH legs must agree; the falsification leg checks the seam)

- **C5 `control` rows.** `RecordRow` (`src/lib/record-sets.mts:38`) gains `control: boolean` (false for every seed/specimen row) and `seedEntry: null` on control rows. The field rides every row artifact the pipeline emits: `artifacts/lowering-rejects.json.lowered[]` (`src/lower.mts:1030-1041`), `.rejects[]`, `artifacts/facts-index.json.bundles[]` (`src/facts.mts:518-538`, present-as-`false`) and each bundle's `provenance` (there ONLY when true — the `malformedFactsControl` shape — so the 24 committed `facts/**` stay byte-identical; the TS leg's reading, adopted), the TS `differential-report.json.pairs[]` and the Go `wazero-pairs.json.pairs[]` (beside `seedEntry`, present-as-`false`, never omitted). **Cardinality asserts stay DERIVED from the loaded set** (they already are — `lower.mts:841-851`, `:914-925`, `build-wasm.mts:180-187`, `certify.mts:991-992`, `:1049-1058`, `certify-verify.mts:270-278`, `compare.mts:850-852`, `wazero-probe/main.go:273-288`, `report.go:341-354`): on `seed20` they count 23 (22 + 1 control) consistently on every arm; the split that matters to the scorer is the manifest's — `records[]` (the 22 seed rows, unchanged shape) vs the new `controlRecords[]` — and the `control` flag on every row. Go's `loweredRecord` (`main.go:76`) ignores additive fields by design (`:71-75`), so the flag passes through; `isRequired` (`recordset.go:56-61`) stays "all" on `seed20`.
- **C6 `controls.json` v1** (owner-defined per the 19:23Z ruling; scorer folds): `artifacts/controls.json` = `{ generatedBy, contract, schema: 'seed20-target.controls.v1', apparatusPin, runCommit, recordSet, artifactsSubdir, controls: [ { id: 'K1'|'K2'|'K3'|'K3b'|'K4'|'K5'|'K5b'|'K6'|'K7'|'K8', refusing: boolean, ok: boolean|null, detail: string, rows?: [...], evidence: [{ artifact, path }] } ], checks: [...] }`. `ok: null` = NOT MEASURED (the input the control needs is absent — K4/K7 without their homes, K3 arm B without a control-only run), with `detail` naming the absent input; `ok: false` on a `refusing` control is an apparatus FAIL check (the stage exits 1). K8 rows `{ id, file, sha256, expect, kind: 'unknown-key'|'missing-mandatory', rejected: boolean, message }`. `evidence[]` points at the inline home each row summarises (the homes are unchanged: `certification-report.json.classifierUnitControls` / `.allHostsFailureRule` / `.blockedClasses`, `opa-abi-census.json.controls[]`, `shipped-verdicts.json.nullEmptySplit`, `differential-report.json.checks[]` / `.errorRows`, `lowering-rejects.json.rejects[]`, `lowered/<pkg>/globs.json.perGlobProbes`, `wazero-report.json.errorRows`, `facts-index.json.bundles[]`).
- **C7 `errorRows` grammar is identical on the two comparators.** Go publishes `wazero-report.json.errorRows = { contract, k5bControlFixtureIds: [...], perArm: { wazero: { total, fromK5bControl, outsideK5b, outsideK5bFixtureIds: [...] } } }` — the exact TS shape of `src/compare.mts:935-957` — plus the same two checks (`:961-975`: zero outside K5b; the control produced exactly one error row on the arm when it is present). Keyed on `malformedFactsControlFixture` (`wazero-probe/compare.go:272`).
- **C8 Go `checks[]` rows are `{ name, ok, detail }`** (`compare.go:605-609 checkRow` — json tag `passed` → `ok`), matching `src/lib/spike-env.mts:299`. `detail` stays `omitempty`.
- **C9 `explanationClass`** on `wazero-pairs.json.pairs[]`: `*string`, json `explanationClass`, present-as-`null` on MATCH rows (the `seedEntry` C4 rule), `"MALFORMED-FACTS-CONTROL"` where `compare.go:376` sets the control explanation, the ordinal class name where `:488` sets `ordinalExplanation` — the class strings are named constants next to the explanations, and the TS side's class vocabulary (`differential-report.json.explanationClasses[]`, `src/compare.mts:1016-1036`) is the reference: the Go `explanationClasses[]` (`pairs.go:51-59`) lists the same ids.
- **C10 `SPIKE_ARTIFACTS_SUBDIR=<name>`** (new, ALL THREE arms — TS, Go, and the Rust host: `host/src/main.rs` hardcoded `artifacts/` at its lowering read, facts read and verdict write until this slice; the TS leg found it and the owner fixed it, so `differential`'s `host:opa`/`host:regorus` follow the subdir too): TS `ARTIFACTS_DIR = join(SPIKE_ROOT, 'artifacts', subdir)` (`src/lib/spike-env.mts:20`) and `REGO_BUILD_DIR = join(SPIKE_ROOT, 'rego', 'build', subdir)` (`src/lower.mts:57`; `src/certify.mts:65/68/69` stop re-declaring roots and import them), `LOWERED_PUBLISH_DIR`, `FACTS_DIR`, `CHAINS_DIR`, `BLOCKED_DIR` all derive from `ARTIFACTS_DIR`; Go reads the same env and applies it to its input root (`<root>/artifacts/<subdir>`) and its output dir (`<root>/wazero-probe/artifacts/<subdir>`, `report.go:552`); for `rego/build/<subdir>/` Go does NOT rewrite paths — it reads each lowered row's `dir` verbatim (the "READ, never re-derived" invariant, `main.go:629-635`; TS already writes `dir` relative to the subdir'd build root, `lower.mts:1037`) and REFUSES any row whose `dir` is not an immediate child of the resolved build root (refusal class `REGO BUILD ROOT` — the Go leg's correct reading of this clause, adopted); `certify.mts:525`'s `go run . -conformance` spawn passes the env through (its explicit `-conformance-out` tmp dir is unaffected by the subdir by design). Unset ⇒ today's paths byte-for-byte. Name validated `^[a-z0-9-]+$`. The manifest records `artifactsSubdir` (`null` when unset).
- **C11 record set `control`** (new, both languages): `RecordSetId` gains `'control'` (`record-sets.mts:24`, `:218-225 activeRecordSet()`; `wazero-probe/recordset.go:34-46 loadRecordSet`, `probe_test.go:271-286` extended, not weakened). `SPIKE_CONTROL_RECORD=<path relative to spikes/spine-adopt/>` selects it; setting it together with `SPIKE_RECORD_SET` to anything but `control` is a refusal on both sides; `facts-index.json.recordSet` (`facts.mts:515`) and the Go identity refusal (`main.go:227-240`) carry the new value unchanged.

## Deliverables

- **S1 — G2 is the generator; the 38 literals go.** In `src/lib/record-sets.mts`: delete `SEED20_EXTRA_PROBE_PATHS` (`:310-351`); keep `SPECIMEN_PROBE_PATHS` (`:277-300`) byte-identical (it is a chain-digest input — comment `:266-276`). Add `seedGlobCensus(): { kind: 'fileGlobs'|'excludeGlobs', glob }[]` reading `target.scope.{fileGlobs,excludeGlobs}` out of the YAML text of `seed/records/*` the way `:132 ruleIdOf` / `:145 languageOf` already do (**`record-sets.mts` must not import `core`** — `records.mts:148 intakeRecordSet` imports from it; a cycle); `distinctSeedGlobs()` (33 at the pin: the 30 `fileGlobs` ∪ the 3 exclude-only `**/*.spec.*`, `**/*.test.*`, `packages/cli/src/commands/**/*.ts`); `matchingProbe(glob)` = every `**/` → `k3b/` FIRST, then every `*` → `probe` (`?`/`{` are literals in the dialect, `lower.mts:164-166`; a bare `**` not followed by `/` is unreachable on this seed — refuse it loudly rather than lower `.*` to `probeprobe`); `nonMatchingProbe(glob)` = the matching probe with the glob's **last literal character** (scanning right-to-left; `*` and `/` are not literals; a character a wildcard produced is not a literal) replaced by `_`, by `-` when it is already `_` — the rule strategy verified 33/33 on both arms and the owner reproduced (`.zz` fails on `**/*.spec.*` and `**/*.test.*`; a prefixed segment fails on 25/33); **no literal in the last segment ⇒ throw** (the "matches everything in that directory" exclusion). `generatedSeedProbes(): { glob, kind, probe, twin }[]`. `sharedProbePaths('seed20')` (`:354`) = `SPECIMEN_PROBE_PATHS` + every probe and twin, deduped, sorted by codepoint. **The pinned expectation:** `seed/probe-pairs.json` — the 33 rows the generator produces at the pin, committed; `verify-records.mts` asserts `generatedSeedProbes()` deep-equals it (this repo's TS side has no test runner — pinned artifacts + `Checks` are its tests). `src/lower.mts:756-772` (per-glob discrimination) must pass with `blind = []` on every reachable glob; the lowerer additionally asserts, per generated pair, that the lowered regex accepts the probe and rejects the twin (`checks.eq` over all 33). `lower.mts:706-751`'s sweep now covers the generated set (its parity checks `:729`/`:739` are the K3b "agreement" predicate — a disagreement there is a T5 `scope-divergence`, reported as a FAIL check).
- **S2 — manifest deltas (`src/manifest.mts`, object-literal order `:192-240`; every new key lands inside the `runManifestSha256` preimage by construction, `spike-env.mts:128-134`).** New keys, in this order after `probePaths`: `probePathsSha256` (sha256 of the sorted `probePaths` list, `\n`-joined + trailing `\n` — the `tracked-paths.txt` convention `:171-176`); `probeGeneration: { rule: '<one-line statement of S1's two rules>', globCount, pairs: generatedSeedProbes() }` (`[]` on `specimens`); `controlRecords: [{ file, sha256, control: true, ruleId, package, role: 'K5' }]`; `controlFixtures: [{ id, file, sha256, expect }]` (K8); `k3Capture: { policyRegoSha256, policyRegoCodeSha256, chainSha256, files: { policyRego, chain } }` (the three digests FLAT — the spellings v1.1 binds to, strategy's 19:32Z; `files` carries the two copy paths) — `policyRegoCodeSha256` is the § 5 comment-stripped form emitted by the apparatus (code lines = the published `policy.rego` minus lines whose first non-blank char is `#` and minus blank lines; the digest the owner computed read-only at 0849Z becomes the apparatus's own claim); `byteIdentity` (§ S4); `trackedPathsAtRecordPin: { pin: '2a7135762b6aedc9cd3099ab3f42e029ee34092e', present: boolean, count, sha256, file: 'tracked-paths-at-record-pin.txt' }` (§ S6; `pin` is the FULL hash, the same form as `recordPin` — v1.1 writes the 8-char prefix; the apparatus never abbreviates a pin, and the PR-open mail states it; `file`, not `list`, is the bound spelling); `artifactsSubdir`; `controlRecord` (the `SPIKE_CONTROL_RECORD` path or `null`). `trackedPaths` (`:209-215`) is **untouched** — T17 provenance at `runCommit`. `probePaths` stays a flat `string[]`. `fillManifestBundles`' digest-stability guard (`spike-env.mts:251-265`) must keep passing. `facts-index.json.bundles[]` rows gain `swapped: boolean` (today a K4 run is invisible to any reader of the index — `facts.mts:518-538` projects only `malformedFactsControl`).
- **S3 — K5 control record in the `seed20` set.** `loadRecordSet('seed20')` (`record-sets.mts:160 seed20Set()`) appends ONE row built from the `d-file` specimen declaration (`src/lib/specimens.mts:108`: same `id`, `ruleId`, `class`, `inlineFilePath`, `exemplarFactory`; `recordFile` = `seed/controls/k5/d-requires-file.rule.yaml`; `packageDiscriminator: 'd-file'`; `seedEntry: null`; `control: true`; `fixture: null`). It flows through intake, lowering, build, certification, facts, the three TS arms and the Go arm exactly like a seed row; `src/facts.mts:355`'s property selector then finds it **unchanged** and mints the two M3 bundles (`:362-396`) into the run's own artifact set; the SKIP arm (`:397-407`) becomes unreachable on `seed20` and stays for `specimens`-without-`d-file` (never the case today) and for `control`. The package is `totem.spike.r<ruleId of d-file>` (`lower.mts:248-250`) — it cannot collide with a seed package (no seed row carries that hash; assert it). Not a K3b glob source (`seedGlobCensus` reads `seed/records/` only — the control's globs are swept beside as the specimens' are).
- **S4 — K6 byte-identity + environment header.** `src/lib/baseline-pins.mts` exports `BASELINE_PIN = '6ca24d42'` and the expected digests, computed by the leg from `git show 6ca24d42:<path>` and pinned as constants: `packages/core/src/spine/rule-record.ts` `34af45324b0d8298855ca4de6b20ee16089e2437d866267082c802ecd8faf1ff`; `packages/core/src/spine/record-lower.ts` `63a8415e7095ba06f65cee6185c84b9008e42399f5c507b9c8b86c71729a937e`; `spikes/spine-adopt/rego/LOWERING.md` `d031189ed6a412e088f747b5e2a2f1ed8b6d8ec3e189db6efcffa775b31fd5f7` (whole file — identical at both pins) plus the `## Input contract` heading line (`LOWERING.md:10`) re-located by `factSchemaLine()` (`lower.mts:99-114`); `spikes/spine-adopt/toolchain.lock` **`[opa]` section only** (from the `[opa]` line to the next blank line — the whole file legitimately differs between the pins: Go/wazero rows were added) — digest computed by the leg from the `6ca24d42` blob with the same extractor; `src/lower.mts` **emitter region** = the source text of `emitPolicy` extracted **by symbol** (from the line `function emitPolicy(` to the line before the next top-level `function`/`export`/comment-block start) — digest computed by the leg from the `6ca24d42` blob with the same extractor (line numbers differ between the pins; symbols don't). The manifest's `byteIdentity = { baselinePin, files: [{ path, region: 'file'|'section:[opa]'|'symbol:emitPolicy', expected, actual, eq }] }` (`files` / `expected` / `actual` are the spellings v1.1 binds to — strategy's 19:32Z; `region` is additive); `manifest.mts` adds one `checks.eq(<path> byte-identical to BASELINE_PIN, actual, expected)` per row — a delta is a FAIL check (the run refuses; benignity is the owner's to state in the record, never the apparatus's to wave through). `recordLowerSha256` (`:235-238`) stays (it is the same bytes; keep both — the scorer binds to symbols). **Environment header:** `manifest.mts` prints, before any check, one block — `record set · runCommit · workingTreeClean · recordPin · artifactsSubdir · controlRecord · BASELINE_PIN · platform · toolchain (opa/node/go/rust/wazero/wasmtime/regorus/opaWasm/astGrepNapi, resolved) · byteIdentity eq summary` — and repeats the same block in the Go arm's run-start (`report.go:232` already prints `record set`). Every `toolchain.*.version` null is a FAIL check on `seed20`/`control` (recorded-not-gated stays for `specimens`).
- **S5 — `SPIKE_CONTROL_RECORD` (K3 arm B, the control-only re-pin build).** With `SPIKE_CONTROL_RECORD=records/c-supp-astgrep-compound-failopen-catch.rule.yaml` (any of the seven specimen files by path; refuse anything else) the record set is `control` (C11) with exactly one row from the specimen table (`shared: false`), `SPIKE_ARTIFACTS_SUBDIR` defaults to `k3-control` when unset, and the seam is `npm run manifest && npm run lower && npm run build-wasm && npm run certify` — the roots come from C10, so the seed's roots are never wiped. INV 2 (`certify-verify.mts:333 committedChainsApply`) becomes `recordSet !== 'seed20'` restricted to the loaded packages: on `control` it compares the one published chain against the COMMITTED `artifacts/chains/<pkg>.json` — that comparison IS K3 arm B (byte identity at the pin), and `controls.json` K3 reads it. `facts`/`shipped-verdicts`/hosts/compare/`verify-records` are not part of the control seam (nothing to differential); `manifest.mts` records the control file's sha in `controlRecords[]` (`role: 'K3'`). Usable control records are `a` · `b` · `c` · `c-supp` only: the twinned `d-line`/`d-file`/`e` lower to `r0123456789abcdef` under `shared: false`, for which no committed chain exists, so INV 2 arm B fails loudly by design. Consequences the seam needs, all folded: `certify.mts` guards its facts-dir read; POST_FACTS artifacts take `bundlesSha256 = sha256([])` on the `control` set only; the census read falls back to the base artifact root.
- **S6 — T5 sweep deposit (`src/t5-sweep.mts`, `npm run t5-sweep`, placed in `differential` right after `lower`).** Enumerate the record-pin tree in-apparatus: `git ls-tree -r --name-only 2a7135762b6aedc9cd3099ab3f42e029ee34092e` (2,742 at the pin, re-derived by the owner 2026-08-29) → `artifacts/tracked-paths-at-record-pin.txt` (same writer convention as `manifest.mts:171-178`) + `manifest.trackedPathsAtRecordPin` (written by `manifest.mts`, so `t5-sweep.mts` reads the list back and re-checks its sha). Pin absent locally (`git cat-file -e <pin>^{commit}` non-zero — the `verify-records.mts:429` skip class): `present: false`, the sweep writes `status: 'SKIPPED — record pin commit not present locally'` with a named always-true check and NO rows; the scorer refuses a SKIPPED sweep on the run of record. For every lowered package: **ONE `opa eval`** — `<OPA_BIN> eval --format=json --strict-builtin-errors -d artifacts/lowered/<pkg>/policy.rego -d <wrapper> -i <input.json> data.t5sweep.in_scope_paths`, where `<wrapper>` is `rego/build/<subdir>/<pkg>/t5-sweep.rego` generated from the template `rego/t5-sweep.rego.tmpl` (committed; `import rego.v1`; `in_scope_paths := {p | some p in input.paths; data.<pkg>.in_scope with input as {"file": p}}` — the emitted `in_scope` has NO `default` (`lower.mts:346-349`), so a non-matching path leaves it undefined and an object comprehension would drop it silently; the set form is the correct reading) and `<input.json>` = `{ paths: <the record-pin list> }`. Measured by the owner on OPA v1.20.0 against the real `r87aff037d7de47a7` policy: 2,742 paths in 99 ms, 362 in scope, per-path `opa eval` cross-checks agree. The helper is `build-wasm.mts:100-106 opa(args, cwd)` hoisted into `src/lib/opa.mts` (`certify.mts:798-804` is its byte-identical duplicate — both call sites import the hoisted one). Shipped side: `core.ruleAppliesToFile(cs.rule, p)` over the same list (`records.mts:37-52 loadCore()` → the built dist barrel; add `ruleAppliesToFile` and `recordScopeMatchesFile` to `requireSymbols`, `:39-50` — both are called today, `lower.mts:713/:725`, and neither is required). Deposit `artifacts/t5-sweep.json` = `{ generatedBy, contract, status, header: { recordPin, treeCount, treeSha256, probeCount, invocation: '<the exact argv template>', mode: 'batched-set-comprehension', opaVersion, wrapperTemplateSha256, packages: [{ pkg, recordId, seedEntry, control, policyRegoSha256, wrapperSha256 }] }, rows: [{ pkg, inScope: [...sorted], shippedApplies: [...sorted], disagreements: [{ path, in_scope, ruleAppliesToFile }], elapsedMs }], probeRows: [<the same shape, swept over manifest.probePaths[]>], checks }` (the `rows` / `probeRows` spellings and the per-package sorted-list form are what strategy accepted 19:57Z; the per-path pair `{in_scope, ruleAppliesToFile}` the charter names is derivable — `in_scope = path ∈ inScope` — stated in `contract`). **A non-empty `disagreements[]` is DEPOSITED, never refused** (strategy 19:57Z, governing the charter's § 3 T5: each entry is that record's `scope-divergence`, a typed miss the scorer assigns per package; the count is a named always-true check row so it is visible). The apparatus refuses ONLY on apparatus fault: an `opa eval` non-zero exit, a lowered package with no `rows[]`/`probeRows[]` entry, or a set member that is not a corpus path. Add to `POST_FACTS_ARTIFACTS` (`spike-env.mts:176-185`). A `checks.eq` cross-check: for 8 sampled paths per package (the first 4 in-scope, the first 4 out-of-scope), the per-path form `data.<pkg>.in_scope` with `-i {"file": p}` agrees with the batched set.
- **S7 — `controls.json` (`src/controls.mts`, `npm run controls`).** A THIRD entry script, run after `differential` AND after the Go probe (the seam order § Verification states; it is the only position at which every source below exists). Rows, each with `evidence[]` (C6): **K1** — the two census evidence rejects (`lookaround` `bddfbd2ec1c75eaf`, `backreference` `80192e6ac2a1dd3c`, `lower.mts:779-793`) appear as G3 reject rows with `class` named, PLUS the seed's own `0e01112d` (`\bnew\s+(?!Totem)Error\(`) rejects at `target-lowering` with `class: 'lookaround'` (`ok` = all three present with the class); **K2** — `certification-report.json.allHostsFailureRule` shows `certfix_neg_lookahead` BLOCKED `empty-result-set`, hosts wasmtime `empty-result-set` · regorus `eval-error` · wazero `empty-result-set` (host-level kinds — `certify.mts:219-225 BLOCKED_REASONS` is a different vocabulary; the row states which it read), `artifacts/blocked/certfix_neg_lookahead.json` `chain: null`; **K3** — arm A: the seed run's `artifacts/chains/r87aff037d7de47a7.json` vs `seed/controls/k3/k3-target.chain.json`, every leaf path in either object compared, `rows` = the differing paths each tagged `identical-expected` / `expected-to-differ` / `UNCOVERED`, with the expected-to-differ list = the charter's + `seedEntry` (an ADDED key IS a differing field — 19:23Z ruling) — `ok` = no UNCOVERED path; arm B: `artifacts/k3-control/chains/<pkg>.json` + `lowered/<pkg>/policy.rego` byte-equal to the captures (`ok: null` "control-only run not present" when `artifacts/k3-control/` is absent); the manifest's `k3Capture` shas re-verified against the files; **K3b** — from every `lowered/<pkg>/globs.json.perGlobProbes`: each reachable glob has ≥ 1 matching and ≥ 1 non-matching probe, `rows` per glob (a `seed20` measurement: on `specimens` it is `ok: null` — the frozen `SPECIMEN_PROBE_PATHS` leaves specimen `a`'s four globs with 0 matching probes, the same reason `lower.mts:756` gates its own per-glob check); **K4** — `artifacts/k4-swap/` present ⇒ its `facts-index.json.bundles[].swapped` true for the record, its `differential-report.json` shows T7 failing for that record while every pair still MATCHes (`ok`), else `ok: null`; **K5** — the control record's two M3 bundles (`-control-unreadable`, `-control-empty`) each `fired: true · matchCount: 1 · error: null` on `shipped-verdicts.json` and MATCH on `differential-report.json` shipped–opa and `wazero-pairs.json` opa–wazero; **K5b** — `differential-report.json.errorRows` AND `wazero-report.json.errorRows`: `outsideK5b == 0` on every arm and exactly one error row from the control per arm; **K6** — `manifest.byteIdentity.files[].eq` all true, every `toolchain.*.version` non-null, `workingTreeClean` recorded (`ok` = the byte rows; the clean-tree fact is `detail`, refused by the scorer not here); **K7** — `artifacts/k7-rebuild/chains/*.json` present ⇒ per-package `regoSha256`/wasm sha equality vs `artifacts/chains/`, `refusing: false`, else `ok: null`; **K8** — parse the two K8 fixtures through `core.parseRuleRecord` + `compileRuleRecord` (`records.mts:59-80 tryCompileSpecimen`'s exact path, including `PINNED_NOW`): a rejection whose message names `fileGlob` (the unknown key at depth — zod `unrecognized_keys` through `rule-record.ts:966-969`) and `message` (the absent § Design 5 mandatory field, `rule-record.ts:740`) respectively; **acceptance is a control FAIL**; the rows carry the full rejection `message` verbatim. `controls.json` embeds `runManifestSha256` + `bundlesSha256` (add to `POST_FACTS_ARTIFACTS`). On `specimens`, rows whose inputs are seed-only report `ok: null` with the reason — the baseline gains the file additively.
- **S8 — K4 / K7 homes.** K4 = `SPIKE_SWAP_EXAMPLES=87aff037-fail-open-catch-ban SPIKE_ARTIFACTS_SUBDIR=k4-swap npm run all && npm run differential` (the flag compares the full row id, `src/facts.mts:289`; a prefix refuses loudly) (+ the Go probe, same env) — a complete second artifact set under `artifacts/k4-swap/` and `wazero-probe/artifacts/k4-swap/`; no code beyond C10 + S2's `swapped` projection. K7 = the Linux arm's chains (the G9 workflow's uploaded `artifacts/chains/**` at the run pin) copied by the owner to `artifacts/k7-rebuild/chains/` at the run of record — no apparatus code; `controls.mts` reads the dir if present. Nothing under either home is committed by this slice.
- **S9 — Go arm (`wazero-probe/**`).** C7 `errorRows` published (`report.go:484-490`becomes the accounting; add the two`ck`checks); C8`checks[]`→`ok`; C9 `explanationClass`; C10 subdir on input root + output dir; C11 `control`record set; the run-start header (S4). Tests:`TestPairsClassification` (`probe_test.go:485`) gains `explanationClass` assertions on the control (`:514-533`) and ordinal branches; `TestWritePairsArtifactShape` (`:636`) pins `explanationClass: null`on MATCH rows and the`control: false`field;`TestAbsentSeedEntrySerialisesAsNull` (`:726`) is re-pinned for the new key count with the same byte-level method; `TestLoadRecordSetReadsTheSharedSelector` (`:271`) accepts `control`and still rejects`seed-20`; new: an `errorRows`accounting test (one control error row ⇒`{1,1,0}`; a non-control error row ⇒ `outsideK5b: 1`and the check fails); a subdir test (env set ⇒ both roots move).`gofmt -l .`empty;`go vet`; `go test -count=1 ./...` green.
- **S10 — seam + workflow.** `package.json`: `t5-sweep` and `controls` scripts; `differential` = `lower && t5-sweep && build-wasm && certify && host:opa && host:regorus && compare`. `.github/workflows/spine-spike-linux.yml`: after `go run .`, add `npm run controls` (working-directory `spikes/spine-adopt`) so the Linux arm deposits `controls.json` too; nothing else changes (SHA-pinned actions untouched). `spikes/spine-adopt/.gitignore`: no change is needed (nothing ignores `artifacts/<subdir>/`) — state it in the PR body rather than relying on it silently.

## Out of this slice (named, with the reason)

`score.mjs` and every verdict (strategy's); the run of record and every K-control RUN at the pin (they run after this slice merges, on the operator's word — § Verification exercises the mechanism locally, commits nothing from it); the § 7 fidelity fix and the `fileText` guard (byte-stability); `1a7080eb`/`0e01112d` re-authoring; the orphan `artifacts/wasm-validation-mutation.json` (committed, no producer — a finding for the PR body, not this slice); hoisting the Rust host's `join_is_sound` (`host/src/main.rs:774-795`) to the Go arm's specimen-exact form (a finding for the PR body); the permissions baseline (mmnto-ai/totem#2695) and the leg-floor mechanism (mmnto-ai/totem#2698) — both queued behind the pin.

## Verification (the slice's own gate, before any PR)

- **Specimens baseline, no env:** `npm run all && npm run differential && (cd wazero-probe && go run . -spike-root ..) && npm run controls` — every stage `rc=0` captured per stage (never through a pipe); `git status --porcelain` on the two artifact trees shows ONLY: run-identity churn (`runCommit`, `generatedAt`, `runManifestSha256`/`bundlesSha256` stamps, `tracked-paths.txt`, `wazero-report.json.timingsMicros.*`), the new manifest keys (S2 — `probeGeneration: []`, `controlRecords: []`, `controlFixtures`, `k3Capture`, `byteIdentity`, `trackedPathsAtRecordPin`, `artifactsSubdir: null`, `controlRecord: null`), `facts-index.json.bundles[].swapped`, `control: false` on row artifacts, the wazero grammar (C7 `errorRows`, C8 `ok`, C9 `explanationClass: null`, the second `explanationClasses[]` entry), `certification-invariants.json`'s INV 8 check NAME (it enumerates the scanned `src/**` modules — 15 → 19, structurally forced by the four new files), and the two additive files `t5-sweep.json` + `controls.json` (+ `tracked-paths-at-record-pin.txt`). After fold 1 the allowance also carries (N4): `probePathsSha256`; `probeGeneration` as `{ rule, globCount, frozen[22], pairs[] }` (`pairs: []` on specimens); `k3Capture.repinned`; `byteIdentity.files[]` at 11 rows (4 file/section + 7 `symbol:` rows, each with `disclosed`); `controlRecords[].seedEntry` + `bundleFixtureIds`; `t5-sweep.json.header.{argv, policies, controlPackagesSkipped, probeCount}` + top-level `timings` (no timing inside `rows[]`/`probeRows[]`); `differential-report.json.pairs[].explanationClass: null` on every MATCH row; `wazero-report.json.recordSet` and its C7/C3 check rows; the drift check rows' disclosing text in `lowering-rejects.json`. **`facts/**`, `chains/**`, `blocked/**`, `lowered/**` byte-identical** (INV 2 green; the K3 capture shas reproduce). Every other byte is a defect to report, not commit. The baseline is regenerated and committed LAST, and the PR body lists the delta key by key.
- **`SPIKE_RECORD_SET=seed20`, same seam:** 23 rows loaded (22 + the K5 control), 2 reject rows (`1a7080eb` shipped-compile; `0e01112d` target-lowering `lookaround`), the K5 M3 bundles minted and MATCHing on every pairing, the K5b control carved out on all four arms with `errorRows.outsideK5b == 0` on each, `probe-pairs.json` reproduced (33 pairs, `blind = []`, every probe accepted and every twin rejected by its lowered regex), `t5-sweep.json` with `status: MEASURED`, `rows[]` over the 2,742 tree paths and `probeRows[]` over the 110 `probePaths[]` (fold 1 F4: frozen 22 + inline 22 + fixture 1 + generated 66 − 1 duplicate), one entry per SCORED lowered package (the K5 control package is skipped and named in the header — fold 1 F7), 0 disagreements deposited on this seed (a non-empty list would be deposited, never refused; report the wall time), `controls.json` with K1 · K2 · K3-arm-A · K3b · K5 · K5b · K6 · K8 `ok: true` and K4 · K7 · K3-arm-B `ok: null` (their homes absent), then `SPIKE_CONTROL_RECORD=records/c-supp-astgrep-compound-failopen-catch.rule.yaml` seam ⇒ `artifacts/k3-control/` with INV 2 green (arm B byte identity) and a re-run of `npm run controls` on the seed set now showing K3 arm B `ok: true`; then a K4 run into `artifacts/k4-swap/` and `controls` showing K4 `ok: true`. Then **restore both artifact trees and delete every `artifacts/<subdir>/`** (constraint 1).
- **Go:** `gofmt -l .` empty, `go vet ./...`, `go test -count=1 ./...` green on `specimens`, `seed20`, `control`.
- **Repo gates (full workspace, not filtered):** `pnpm exec prettier --check` on every changed file, `pnpm exec eslint` on changed TS/MTS, full-branch `totem lint`, `pnpm -r build`; a changeset is NOT owed (the spike is not a published package; nothing under `packages/**` changes except nothing — confirm `git diff --stat main -- packages/` is empty).
- **Falsification leg** on the full diff (constraint 7), findings folded; then the Linux workflow green on the push; then the PR — and the owed mail to strategy-claude when it opens.
