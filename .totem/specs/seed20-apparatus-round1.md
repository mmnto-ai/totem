# seed-20 apparatus — bot round 1 fold (mmnto-ai/totem#2694, reviewed head `25c2a705`)

Dispositions ruled by the operator 2026-08-29 on the apparatus-runner's recommendations; every finding below was verified at source before this fold was written. Two build legs implement (TS · Go), a scoped Opus falsification leg reads the fold BEFORE it is pushed (the standing pre-merge leg was owed on the first presentation and skipped — corrected here). Constraints of `.totem/specs/seed20-apparatus.md` stand unchanged: no run of record, no seed20 artifacts committed, the 7-specimen baseline plus additive files only, the § 7 fidelity fix deferred and the lowerer's EMITTED POLICY byte-stable (K3's chain sha `r87aff037d7de47a7` must still reproduce).

## Declined (with grounds; recorded for the disposition, no code)

- **GCA 11/12** — `json.Unmarshal` of `fired: null` / `matchCount: null` into Go `bool`/`int` "fails". Refuted: the Go spec makes a JSON null into a non-pointer scalar a no-op; measured (`err=<nil>`, values untouched); the seed20 probe run read that exact row and passed 21/21.
- **CR 2, 16** — rewrite the patterns of `5da43ea6`, `be74c55c`, `6b2b62eb`. These are byte-identical copies of the frozen seed (constraint 5); their defects are the scorer's typed dispositions (`be74c55c`/`6b2b62eb` = `defective-source` discards; `5da43ea6`'s locus seam = mmnto-ai/totem-strategy#1093).
- **CR 1** — add a `fileText` string-or-null clause to the generated `facts_wellformed`. True, but it changes every emitted `policy.rego` (all seven committed chains, INV 2, K3) — the same class as the § 7 deferral. Tracked for the promotion slice beside § 7; not this trial.

## Cross-cutting contract (BOTH legs must agree; the falsification leg checks the seam)

- **C1 `recordSet` identity on the fact corpus.** `facts-index.json` gains a top-level `recordSet: "specimens" | "seed20"` (TS writes it from `activeRecordSet()`); the Go probe reads it and REFUSES when it differs from `SPIKE_RECORD_SET` (before any cardinality or join check).
- **C2 designed-error predicate for the K5b control (`m3-malformed-lines`).** Each arm has ONE designed error for that bundle: `shipped` → error text begins `MALFORMED FACTS`; `opa` (wasmtime) and `wazero` → error text contains `EMPTY RESULT SET`; `regorus` → contains `UNDEFINED`. A pair on that fixture is `EXPLAINED-DIVERGENCE` (`MALFORMED-FACTS-CONTROL`) iff BOTH sides carry non-empty errors AND each side's error matches ITS OWN arm's designed error; any other pairing of errors (a trap, a decode failure) is `UNEXPLAINED-DIVERGENCE` carrying both messages. Implemented in `src/compare.mts` (shipped–opa, shipped–regorus, opa–regorus) and `wazero-probe/compare.go` (opa–wazero, shipped–wazero) with the same texts; the predicate lives in ONE named place per language (not in a test file).
- **C3 empty error string is NOT an error row** on any arm: TS `typeof r.error === 'string' && r.error.length > 0`; Go already requires non-empty (keep it; the existing `TestShippedEmptyErrorStringIsNotAnErrorRow` pins it).
- **C4 `seedEntry` absent ⇒ JSON `null`** in every artifact, TS and Go alike (Go: `*string`, nil when empty).

## TS leg — file set `spikes/spine-adopt/src/**` (+ `package.json` only if a script must change)

- **T15 (Greptile P1) manifest identity binds the bundle set.** Keep `runManifestSha256` as the pre-facts identity (its preimage still excludes `bundles`). After `fillManifestBundles` writes `bundles[]`, compute `bundlesSha256 = sha256(canonical JSON of the SORTED bundles list)` and store it in the manifest as `bundlesSha256`. Every artifact written AFTER facts (`facts-index`, `shipped-verdicts`, `lowering-rejects`, `opa-abi-census`, `chain-inputs`, `certification-report`, `certification-invariants`, `differential-report`) embeds BOTH `runManifestSha256` and `bundlesSha256`; artifacts written before facts (`manifest` itself, `expressibility-census`, `records-verification`) embed only `runManifestSha256`. `readRunManifest()` consumers that stamp artifacts must fail loud if `bundlesSha256` is missing when they run after facts. Document the two-digest identity in `spike-env.mts` where the single-digest rationale is now.
- **T17 `trackedPaths` from the recorded commit.** `git ls-tree -r --name-only HEAD` (not `ls-files`); add `workingTreeClean: boolean` from `git status --porcelain` (recorded, not gated — the manifest states the fact; a dirty tree is the scorer's to refuse); `source` text updated.
- **T13 committed-chain gate keyed on the manifest.** `committedChainsApply = manifest.recordSet === 'specimens'`, and a check that `activeRecordSet() === manifest.recordSet` (a mismatch is a FAILED check, not a skip).
- **T10 (TS half of C1)** write `recordSet` into `facts-index.json`.
- **T9 (TS half of C2)** `compare.mts`'s `MALFORMED-FACTS-CONTROL` decision uses the per-arm designed-error predicate.
- **T20 (C3)** `compare.mts:155` requires a non-empty error string.
- **T21** `shipped-verdicts.mts` K5b check: replace the literal `true` with a predicate over the row about to be pushed (`fired === null`, `matchCount === null`, `arms.length === 0`, non-empty `error` beginning `MALFORMED FACTS`, and `malformed.length > 0` — a control bundle whose `lines[]` are all strings must FAIL this check, not pass with an empty member list).
- **T22** the `matchCount !== lineCount` assertion excludes error rows (same predicate the regex asymmetry measurement uses).
- **T3** `build-wasm.mts:169`: `Array.isArray(manifest.records)` before `.length`, else throw naming the invalid artifact and `npm run manifest`.
- **T4** `opa-abi-census.json` headline: fixed keys `hostImplementedBuiltinsAcrossAll` / `distinctImportsAcrossAll` plus `censusedPolicies: censuses.length` (this changes one committed baseline key — expected, list it in the PR).
- **T5** `lower.mts:966`: `factBundleFiles.length === 0` ⇒ `reachability: "NOT MEASURED — no fact bundles on disk"` (never "Unreachable on all 0").
- **T6** `verify-records.mts:413`: `spawnSync` `status === null` (git could not execute) or `error` set ⇒ a FAILED check naming the execution failure; only a genuine non-zero exit is the `pin-commit-not-present-locally` skip.
- **T7** `verify-records.mts:434`: enumerate the pinned tree too (`git ls-tree -r --name-only <pin> -- .totem/rules/`, `.rule.yaml` only) and compare BOTH directions; a file at the pin absent locally is a drift row (`<file>: absent locally`).
- **T18** `certify-verify.mts:353`: `changedKeys`/`addedKeys` are `null` when `committedText` is null (like `mode`, `allModeChecksPassed`, `preservedBytes`).
- **T19** `certify.mts:998`: the SENTINEL count check's detail names a count mismatch explicitly (`expected N measured bundles, found M`) when `factFiles.length !== expectedMeasured`, and lists rejects when there are rejects.
- **T4/T15 baseline bytes:** after the fold, the specimens baseline artifacts change ONLY by: the fixed headline keys + `censusedPolicies`; `bundlesSha256` on the eight post-facts artifacts and in `manifest.json`; `recordSet` in `facts-index.json`; `workingTreeClean` + the `ls-tree` source text in `manifest.json`; check-row text changes named above; **(amended after the falsification leg, M4)** the run-identity churn inherent to G5 — `runCommit`, `generatedAt`, `runManifestSha256` in every stamped artifact, `trackedPaths.count`/`.sha256` and `tracked-paths.txt` (+47 lines: the paths `25c2a705`/`4d32f07e` themselves added) — and the published `explanationClasses[MALFORMED-FACTS-CONTROL]` text in `differential-report.json` and `wazero-pairs.json` (aligned to the per-arm predicate by the owner's ruling), plus the compare-stage self-probe check rows (M5). `facts/**`, `chains/**`, `blocked/**`, `lowered/**` stay byte-identical. Every other byte delta is a defect to report, not commit.

## Go leg — file set `spikes/spine-adopt/wazero-probe/**`

- **G10 (Go half of C1)** read `facts-index.json.recordSet`; refuse (exit non-zero, named error) when it differs from `SPIKE_RECORD_SET`; a missing field is refused too (the TS half always writes it now).
- **G9 (Go half of C2)** `compareControlPair`: the per-arm designed-error predicate (`shipped` ⇒ begins `MALFORMED FACTS`; `opa`/`wazero` ⇒ contains `EMPTY RESULT SET`); other paired errors ⇒ `UNEXPLAINED-DIVERGENCE` with both messages; the designed-error texts are named constants in a non-test file (`compare.go`), and `probe_test.go` imports them rather than re-spelling them.
- **G8 (C4)** `pairResult.SeedEntry *string` (`omitempty` is NOT wanted — the key must be present as `null`; use a pointer and let encoding/json emit `null`); `report.go` sets it only when non-empty.
- **G14** keyed fields in the `violation`/`event` literals in `probe_test.go`.
- Tests: extend `TestPairsClassification` with (a) both arms erroring with the DESIGNED texts ⇒ EXPLAINED, (b) opa `EMPTY RESULT SET` vs wazero `some trap` ⇒ UNEXPLAINED with both messages, (c) shipped `MALFORMED FACTS…` vs wazero `EMPTY RESULT SET` ⇒ EXPLAINED (the shipped–wazero pairing), (d) shipped `EMPTY RESULT SET` (wrong arm's error) vs wazero `EMPTY RESULT SET` ⇒ UNEXPLAINED; a test that a `facts-index.json` with `recordSet: "seed20"` under `SPIKE_RECORD_SET=specimens` is refused; a test that an absent seed entry serialises as `null`.
- `gofmt -l .` empty; `go vet`, `go build`, `go test -count=1` green; the 7-specimen probe output stays byte-identical modulo timings EXCEPT `seedEntry: null` (was `""`) in `wazero-pairs.json` — expected, list it.

## Verification the runner performs after both legs (not the legs)

Specimens: `npm run all && npm run differential && go run . -spike-root ..` — baseline byte delta exactly as enumerated above. seed20: the same through the probe — compare 126/3/0, pairs 42/1/0, all checks green, the K5b pair EXPLAINED under C2 on every pairing, then artifacts restored and the specimens baseline regenerated. Then the scoped falsification leg on `git diff 4d32f07e` — findings folded — then gates, push, and the round-1 disposition.
