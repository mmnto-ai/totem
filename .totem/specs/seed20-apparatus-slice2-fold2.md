# seed-20 apparatus slice 2 — falsification-leg fold 2 (re-armed pass on `5f273c73..e3f4ceae`, deposit 2026-08-29 ~21:40Z)

Every fold-1 item verified FOLDED at the cited lines (B2 and F17 PARTIAL, closed here). The new findings and the owner's dispositions. The leg again read strategy's **local** scorer tree (`550fb75`, ahead of the pushed tip, which carries none of the fold-1 keys) — secondary, unstable evidence; used only where the apparatus-side fix is right on its own terms. Implemented by the TS fold leg (`spikes/spine-adopt/src/**`) except H4's Go half (owner).

## Adopted

- **H1 (NEW-1, BLOCKING — the B6 preimage, now measured against the scorer's coded join).** The apparatus MOVES to the scorer's rule, and keeps the file-bytes property by changing the file: `artifacts/tracked-paths-at-record-pin.txt` is published **without** a trailing newline; `trackedPathsAtRecordPin.sha256` = sha256 of the sorted list `\n`-joined, no trailing newline = `sha256sum` of that file; `probePathsSha256` = sha256 of the sorted `probePaths` `\n`-joined, no trailing newline; `t5-sweep.header.treeSha256` = the re-read file's bytes (identical by construction). `tracked-paths.txt` (T17, slice 1) is untouched — the scorer hashes its file bytes as they are. The manifest states the rule once (`digestRule: 'sha256 over the sorted list, newline-joined, no trailing newline — equal to sha256sum of the published list file'`). Carried to the PR-open mail as a decision, not a disclosure.
- **H2 (NEW-2 + NEW-3, MATERIAL — `repinned` is a real predicate, and its shape is the scorer's).** `k3Capture.repinned` is decided by K3 ARM B — the control-only build's bytes vs the captures — never by re-hashing the byte-pinned copies (a tautology). `manifest.mts` reads `artifacts/k3-control/lowered/<pkg>/policy.rego` and `artifacts/k3-control/chains/<pkg>.json` when present: all three digests equal the captures ⇒ `repinned: false`; any differs ⇒ `repinned: true` with the FLAT keys `repinnedPolicyRegoSha256`, `repinnedPolicyRegoCodeSha256`, `repinnedChainSha256`, `repinnedFrom: 'artifacts/k3-control'`; the build absent ⇒ `repinned: null` with `repinnedFrom: null` (the seam order below makes it present on the run of record). The manifest's `repinned === false` FAIL check is removed — the disposition lives in `controls.json` K3. This is also the pushed charter's clause in effect: an emitter delta that changes the emitted bytes changes arm B and re-pins; one that does not (the `packageSuffix` rename) does not.
- **H3 (NEW-5, MATERIAL — the clean-tree gate's scope).** `workingTreeClean` attests that the SOURCE tree equals `runCommit`: `git status --porcelain -- . ':!spikes/spine-adopt/artifacts' ':!spikes/spine-adopt/wazero-probe/artifacts' ':!spikes/spine-adopt/rego/build'`; the excluded trees are the run's own outputs and their state is recorded beside as `artifactTreeEntries: [...]` (T17 provenance, never gated). The manifest records `workingTreeCleanScope` (the pathspec). With this the charter's § 6 sequence (seed run → controls at the pin → K3 arm B → K4 under its home) runs on the run of record with NO `SPIKE_ALLOW_DIRTY_TREE`; the flag stays for dev runs with uncommitted source. The runner's seam drops the flag from every section to prove it.
- **H4 (NEW-10, MINOR→adopted) — K5b staleness keyed on the run, not the set.** The Go arm writes `runManifestSha256` (read from the manifest it already opens for the header) into `wazero-report.json` and `wazero-pairs.json`; `controls.mts` K5 and K5b refuse a report whose `runManifestSha256` ≠ this run's (and keep the `recordSet` check).
- **H5 (NEW-4, MATERIAL) — `EXPECTED_DELTAS` keyed on the digest pair.** `EXPECTED_DELTAS = { packageSuffix: { expected: '<6ca24d42 digest>', actual: '<the HEAD digest>', reason } }`; a row is `disclosed` only when its measured `actual` equals the pinned `actual`; any other `eq: false` is undisclosed and FAILS. The HEAD digest is thereby pinned where the run compares against it.
- **H6 (NEW-6) — the manifest publishes `checks: checks.rows`** (outside the `runManifestSha256` preimage, like `bundles`; `fillManifestBundles` preserves it), so the K6 rows, the F1 code-digest check and the clean-tree row are machine-readable and the charter's "`checks[].ok` false in any artifact" reaches them.
- **H7 (NEW-7) — the T5 SKIPPED arm carries `recordSet` and `elapsedMs`** like the MEASURED arm.
- **H8 (NEW-8) — `INVOCATION`/`argv` name the policy path with the subdir hole**: `artifacts/<subdir>/lowered/<pkg>/policy.rego` (empty subdir ⇒ `artifacts/lowered/…`, stated).
- **H9 (NEW-9) — F5 keyed on the row's `id` (specimen), not `ruleId`; the M3 control ids are declared only for the FIRST `requires.scope: file` control row (mirroring `facts.mts`'s `find`).**
- **H10 (NEW-11) — K8 names the field by KEY PATH:** `tryCompileSpecimen`'s rejection already formats `<file>: <keyPath> — <detail>`; K8 parses the `<keyPath>` segment and requires its last segment to equal the expected field (`fileGlob` under `target.scope`; `message`), with the whole-token test on the detail as the second condition.
- **H11 (NEW-12, NEW-13, NEW-14, NEW-16) —** the tautological sort check asserts `sharedProbePaths`'s output is sorted BEFORE the manifest re-sorts (so it can fail); the inline/fixture-coverage row is emitted only on `seed20`; the docstring about the control's `inlineFilePath` states the fact (`scripts/x.sh` IS in the frozen 22 — it is not appended, it is already there); the F4 memo is keyed by record-set id.
- **H12 (NEW-15) —** the record's seed20 verification line: 110 probe paths; one `rows[]`/`probeRows[]` entry per SCORED (non-control) lowered package (owner's edit).

## Not adopted (with grounds)

- None. NEW-6 was pre-existing and is adopted anyway (H6) because the fold added rows to the invisible set.

## Seam order the runner now proves (no `SPIKE_ALLOW_DIRTY_TREE` anywhere)

specimens check → restore → **control-only build first** (`SPIKE_CONTROL_RECORD=…` ⇒ `artifacts/k3-control/`) → seed20 `all` + `differential` + Go arm + `controls` (the manifest sees arm B present ⇒ `repinned: false` measured; the tree's only dirt is under the excluded artifact trees ⇒ `workingTreeClean: true`) → K4 under `k4-swap/` through all three arms → `controls` again (K3 and K4 `ok: true`) → restore → FINAL specimens regeneration. Then a third, narrowly scoped falsification read on H1–H3, H5 and H10.

## Rulings after the TS leg's report

- **H10 — the leg STOPPED correctly; the premise was wrong twice.** `tryCompileSpecimen` wraps the parser's `[Totem Error] <file>: <keyPath> — <detail>` inside `specimen <id> (<file>) FAILED parseRuleRecord: …`, and zod raises `unrecognized_keys` at the CONTAINER path (`target.scope`), never at the key — so "last segment equals the field" cannot hold for the unknown-key class. **Ruled (option ii):** the EXPECTED dotted path (`target.scope.fileGlob` / `message`) must equal or start with the key path the parser named, AND the detail must name the field as a whole token. Measured on the real messages: unknown-key ⇒ keyPath `target.scope`, detail `target.scope: Unrecognized key(s) in object: 'fileGlob'` ⇒ true; missing-mandatory ⇒ keyPath `message`, detail `message: Required` ⇒ true; the decoy `recoveryHint — message must be non-empty` ⇒ keyPath `recoveryHint` does not prefix `message` ⇒ false. Rows publish `keyPath` and `expectedPath`.
- **Third-read note 1 (`digestRule` scope):** the rule names the keys it governs — `probePathsSha256`, `trackedPathsAtRecordPin.sha256`, `t5-sweep.header.treeSha256` — and states that `trackedPaths.sha256` (T17, slice 1) hashes `tracked-paths.txt`'s bytes, trailing newline included, unchanged.
- **Third-read note 3 (`artifactTreeEntries` granularity):** recorded with `-uall`, file by file.
- **Notes 2, 4, 5, 6:** `:!rego/build` stays in the pathspec as documentation of the excluded tree (gitignored, so a no-op — stated); `repinned: null` is a first-class NOT MEASURED outcome that `controls.mts` K3 already treats as such; H5's pair keying refuses a new delta by design; the trailing-newline-free list file is never touched by prettier (`spikes/spine-adopt/artifacts/` is prettierignored) and `.gitattributes` normalises line endings only, never a missing final newline.
