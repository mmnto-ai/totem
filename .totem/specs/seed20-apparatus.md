# R14 seed-20 compilation-target trial — the apparatus slice (G1–G10)

> **Ritual note.** `totem spec seed20-apparatus` ran (run artifact `.totem/artifacts/runs/f7fb3f89….json`, `caller = spec`) and drafted an unrelated "20 mock context stores" fixture — the same hallucination class as mmnto-ai/totem#2687 and the slice-(2) spec of this session. Replaced by hand; nothing below is LLM-authored.

**Charter:** mmnto-ai/totem-strategy#1154 — `operations/310-seed20-target-preregistration.md`, DRAFT v0.3 @ `40ee2a5` (registers as v1 at merge). Rulings of record: the bus's 2026-08-29T05:09Z fold + 05:25Z v0.3 corrections (both consumed and verified at source by this seat), under the operator's standing word; the operator's direct go on the apparatus slice 2026-08-29 (this session). **Lane:** totem-claude = apparatus-runner. **This slice is APPARATUS, never scoring: it emits rows and artifacts; it decides no verdict.**

## Hard constraints (each one a refusal condition on the PR)

1. **No run of record. Nothing scored. No pin mailed** until mmnto-ai/totem-strategy#1154 merges AND the floor block is ruled on mmnto-ai/totem-strategy#1138. Local runs to exercise the apparatus are allowed; **no seed-run artifacts are committed on this branch** — `spikes/spine-adopt/artifacts/**` and `wazero-probe/artifacts/**` stay at the 7-specimen baseline (`6ca24d42`) until the run of record, which is a later, separate commit at the mailed pin. After any local seed run: `git checkout -- spikes/spine-adopt/artifacts spikes/spine-adopt/wazero-probe/artifacts` (and remove untracked outputs under them) before staging.
2. Branch `r14/seed20-target-apparatus`, cut from main `6ca24d42`; brought up to totem main before the pin is mailed (§ 4 of the charter).
3. **§ 7 fidelity fix DEFERRED** (ruled): `PRECEDING_LINE_MARKERS` in `src/lower.mts:59-68` is byte-stable; the emitted policy for the 7 specimens must stay byte-identical so K3's chain sha (`artifacts/chains/r87aff037d7de47a7.json`) holds.
4. The 7-specimen baseline stays runnable and reproducible: the loader (G1) selects a **record set**; the default is the existing specimens (all existing artifacts/checks/Go tests hold unchanged); `SPIKE_RECORD_SET=seed20` selects the trial set.
5. Frozen inputs: the seed = the 22 `.totem/rules/r14-*.rule.yaml` at `2a7135762b6aedc9cd3099ab3f42e029ee34092e` (branch `r14/seed-20-translation`; 20 rules; the `6b1890e2` and `71935fe9` `language: javascript` twins). The apparatus carries **byte-identical copies** under `spikes/spine-adopt/seed/records/` (obtained with `git show 2a7135762b6aedc9cd3099ab3f42e029ee34092e:<path>`), plus `seed/PIN.md` (the pin, the file list, sha256 per file); the manifest (G5) re-records each sha256 at run time. A check compares the copies to the pinned blobs when the commit is present locally and SKIPS with a named reason when it is not (CI shallow clone).
6. Every comment cites issues as `mmnto-ai/<repo>#N`; never a bare `#N`.

## Deliverables (G1–G10 — the charter's § 2, corrected at v0.3)

- **G1 — record loader + generalized asserts.** Replace the hardcoded `SPECIMENS` table (`src/lib/specimens.mts:47-128`) as the pipeline's source with a loader: `loadRecordSet()` → `Specimen[]` for `specimens` (today's seven, unchanged) or `seed20` (one row per seed file: `id` = file stem minus `r14-`; `ruleId` = the record's `lessonHash`; `class` from the compiled engine; `recordFile`; `legacySource` = the hash8 → the seed entry; `fixture` = `.totem/tests/test-87aff037d7de47a7.md` for `87aff037` only, else null; `inlineFilePath` per G2). **Package naming rule (fixed by the charter):** `totem.spike.r<lessonHash>` for single-record rules; `totem.spike.r<lessonHash>_<language>` for BOTH records of the two twinned rules. Generalize the cardinality/id asserts so they hold for any record set: `lower.mts:764` (`lowered.length === 7`), `:765-769` (7 unique packages), `:820-824` (build tree holds exactly those), `:592-596` (shared-id set is exactly `['0123456789abcdef']`) → counts from the loaded set; the shared-id set = the set's declared twinned ids. Same for every other hardcoded `7`/`24` in `build-wasm.mts`, `certify.mts`, `certify-verify.mts`, `facts.mts`, `shipped-verdicts.mts`, `compare.mts` (census them with grep; each becomes a derived count).
- **G2 — probe coverage.** Every seed record gets an in-scope `inlineFilePath` (ending `.js` for the two twins — ast extraction dispatches on extension, `facts.mts:59-61,83-91`); the shared probe set (`lower.mts:545-573`) is extended so every seed glob has ≥ 1 matching and ≥ 1 non-matching probe (the discriminating check `:710-714` must pass for all 22; today five have zero in-scope paths: `b9736096`, `be74c55c`, `b237bcf3`, both twins).
- **G3 — reject-row path.** A shipped-compile rejection (`records.mts:58-63`, e.g. `1a7080eb`'s unregistered `json` language) and a § Lowering 4 target rejection on a record (`lower.mts:632-636`, e.g. `0e01112d`'s lookahead) are **scored rows, not throws**, for the seed set: `artifacts/lowering-rejects.json.rejects[]` gains rows `{recordId, seedEntry, ruleId, stage: 'shipped-compile' | 'target-lowering', reason, class?}`; the run continues; downstream stages skip rejected records by construction (no bundle, no chain); the existing two census evidence rejects (`:779-793`) keep their shape. A reject row is NOT a failed `checks[]` entry (apparatus health ≠ record verdict). For the `specimens` set the old throw behavior is preserved (the spec says all five specimens lower).
- **G4 — per-seed grouping.** Every row/pair (`lowering-rejects.lowered[]`, `facts-index.bundles[]`, verdict rows, `differential-report.pairs[]`, chains, blocked) carries `seedEntry` (the hash8) beside `specimen`, so a rule's N records group; the scorer aggregates — the apparatus only labels.
- **G5 — run manifest** `artifacts/manifest.json`, written first by a new `src/manifest.mts` (`npm run manifest`, and as step 0 of `all`): `{ recordSet, runCommit (git rev-parse HEAD), recordPin: '2a7135762b6aedc9cd3099ab3f42e029ee34092e', records[{file, sha256, seedEntry, ruleId}], fixtures[{file, sha256}], probePaths[], trackedPaths (git ls-files at HEAD — T5's sweep set; count + sha256 of the sorted list; the list itself under artifacts/tracked-paths.txt), bundles[{fixtureId, sha256}] (filled after facts), toolchain{opa, rust, node, astGrepNapi, wazero, go, wasmtime, regorus, opaWasm — resolved, not declared}, platform{os, arch}, recordLowerSha256 (sha256 of packages/core/src/spine/record-lower.ts — the byte-identity ground the charter names), generatedAt }`. Every later artifact embeds `manifestSha256`.
- **G6 — the backtick self-assert** (`lower.mts:746-750`) tests raw-string SYNTAX, not the byte: strip double-quoted Rego string literals (respecting `\"` escapes) from the code lines, then assert no backtick remains. `5da43ea6` (a backtick inside a character class) must lower and pass; a genuine raw-string literal in emitted code must still fail (add a negative control on a synthetic line).
- **G7 — publish the lowered artifacts.** `policy.rego` + `globs.json` per record are copied to `artifacts/lowered/<pkg>/` by `lower.mts`; `spikes/spine-adopt/.gitignore` un-ignores that path (`rego/build/` stays ignored). T3 audits from these at the pin.
- **G8 — malformed-facts bundle.** One synthetic `synthetic-control` bundle in `facts.mts` (`fixtureId: 'm3-malformed-lines'`, a non-string member in `lines[]`, so `facts_wellformed` is false and `result` undefined) for the K3 record (`87aff037` on seed20; specimen `c-supp` on specimens — or skip on specimens if it would change the baseline artifacts; **it must not change the 7-specimen artifacts**, so gate it on `seed20`). Expected: an error row on wasmtime AND wazero (K5b). `compare.mts` carves it out with a named explanation class (`MALFORMED-FACTS-CONTROL`) so it never renders as `UNEXPLAINED-DIVERGENCE` and never manufactures a `parity-divergence`; the whole-run `errorRows` count reported by the host must exclude exactly this bundle when the summary says "0 outside K5b".
- **G9 — the Linux trigger.** `.github/workflows/spine-spike-linux.yml`: replace `on.push.branches: [wt/spine-spike]` with a `paths: [spikes/spine-adopt/**]` filter (any branch); keep `workflow_dispatch`. Nothing else in the workflow changes.
- **G10 (Go, BLOCKING) — the wazero probe's verdict path over any record set.** `wazero-probe/report.go:292-294,305,380` hard-assert `24` rows on every arm → the manifest's bundle count; `isRequired` (`report.go:200-202`, today `a` and `d-file`) → the record set's required set (for `seed20`: every record; for `specimens`: unchanged); `joinIsSound` (`main.go:149-160`, the `<ruleId>-<specimen>-` filename-prefix join) → G1's package/fixture naming (twins share a `ruleId`, so the join must use the specimen/package suffix, never `ruleId` alone). Emit the comparison in `differential-report.json`'s shape: `wazero-probe/artifacts/wazero-pairs.json` with `pairs[]` keyed `(specimen, seedEntry, fixtureId, left: 'opa', right: 'wazero', status ∈ {MATCH, EXPLAINED-DIVERGENCE, UNEXPLAINED-DIVERGENCE}, detail)` plus a `summary`; `wazero-verdicts.json` keeps the wasmtime row shape. `main.go:168-171`'s empty-result strictness stays (K5b relies on it). `probe_test.go` generalizes with the code. The 7-specimen run must reproduce today's `wazero-report.json` / `wazero-verdicts.json` byte-for-byte modulo the new `pairs` artifact.
- **K4 support (dev flag).** `SPIKE_SWAP_EXAMPLES=<recordId>` in `facts.mts` swaps that record's `bad`/`good` inline bundles (label the bundles' `provenance.swapped: true`) so the scorer's K4 control ("T7 fails while T8 still MATCHes") is runnable without editing records.

## Verification (the slice's own gate, before any PR)

- `npm run all && npm run differential` on the **specimens** set reproduces the committed baseline: `git status --porcelain spikes/spine-adopt/artifacts spikes/spine-adopt/wazero-probe/artifacts` is empty EXCEPT for the additive files this slice introduces (`manifest.json`, `tracked-paths.txt`, `lowered/**`, `wazero-pairs.json`) and any `manifestSha256`/`seedEntry` fields added to existing artifacts — list every changed artifact byte in the PR body and explain each.
- `SPIKE_RECORD_SET=seed20 npm run all && … differential` completes without an apparatus throw: 22 records loaded; `1a7080eb` and `0e01112d` appear as reject rows (stages `shipped-compile` / `target-lowering`); `5da43ea6` lowers (G6); every seed glob discriminating (G2); `m3-malformed-lines` yields error rows on wasmtime and wazero and is carved out by the comparator; the manifest carries every field in G5. Then restore the tracked artifacts (constraint 1).
- Go: `go test ./...` in `wazero-probe/` green on both record sets; `cargo build --release` for the host unchanged.
- Repo gates: `pnpm exec prettier --check` on every changed file, `pnpm exec eslint` on changed TS/MTS, full-branch `totem lint`, `pnpm -r build` (the spike consumes `packages/core/dist`).

## Out of scope (named)

Scoring (`score.mjs`, strategy's); any K-control RUN (they run at the pin, after merge + floor word); the run of record; the § 7 fidelity fix; `wasm-tools`/the mutation check (out of the trial); actuator promotion into `packages/core`; re-authoring `0e01112d`; the R14 harness under `operations-local/`.
