reviews:
  early_access: true
  path_instructions:
    - path: "packages/core/**"
      instructions: "This is the core library — LanceDB store, compiler, rule engine. No CLI or MCP dependencies. All LanceDB interactions must stay in this package."
    - path: "packages/mcp/**"
      instructions: "MCP server for AI agents. Sanitize all inputs. XML-wrap all responses. No shell: true on spawn calls."
    - path: "packages/cli/**"
      instructions: "CLI commands. All imports must be lazy-loaded (dynamic import). log.error() must use 'Totem Error' as the tag."
    - path: ".totem/lessons/**"
      instructions: "Pipeline 1 lesson files. Validate: heading ≤60 chars, required fields (Pattern/Engine/Severity), no markdown-escaped globs."
    - path: ".claude/**"
      instructions: "Claude Code hooks and skills. Bash scripts must use [[ ]] glob matching, not echo|grep. CLAUDE.md must stay under 50 lines."
knowledge_base:
  learnings:
    scope: auto
