# Totem: Gemini CLI Entry Point

The canonical agent instructions for this repository live in [`AGENTS.md`](AGENTS.md).

Following the AGENTS.md convention, `mmnto-ai/totem` uses a single `AGENTS.md` as the source of truth for how all AI coding agents behave here. This file exists only so Gemini CLI finds its way to `AGENTS.md`.

Read `AGENTS.md` before doing anything else.
