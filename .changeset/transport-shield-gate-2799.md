---
'@mmnto/totem': minor
'@mmnto/cli': minor
---

A second gate in the gate engine: `transport-shield` (mmnto-ai/totem#2799) — a PreToolUse gate on the **Bash and PowerShell** tools that refuses the known payload-mangling command shapes before the shell sees them, naming the cure in every refusal. `totem gate install transport-shield` (or `--all`, or `totem init --gates=transport-shield`) installs it under a `Bash|PowerShell` matcher beside `freeze-check`'s `Write|Edit` entry.

**The pattern table lives once, in core beside the evaluator** (`packages/core/src/transport-shield.ts`), and every row is a pure function of the payload `{ tool, command, platform }` — no filesystem, no environment; the host passes its own `process.platform`, so the same payload yields the same verdict on every machine (the verdict may differ BETWEEN platforms — that is the payload doing its job) and a test pins win32 on any runner. Deny rows, in evaluation order, the first to fire wins: `heredoc-escape` (a `<<` / `<<-` body, quoted or bare delimiter, carrying a backslash or a backtick — this transport rewrites both; an unterminated body is read to the end), `msys-body-slash` (win32 + Bash only: a `--body` / `--body=` / `-b` value beginning with `/`, shell-tokenized, so `--body-file` and an `@` body never match), `sed-i-escape` (`sed -i` / `-i.bak` / `--in-place` whose expression operand carries a backslash or a newline inside quotes or a `$( … )`, or more than one `-e`; a `-f` / `-fFILE` / `--file` / `--file=` script path is never the expression, and with a script file no positional operand is), `inline-body-escape` (Bash only: a `node -e` / `--eval` / `-p`, `python -c`, `python3 -c` body carrying a backslash or a backtick). Warn rows, consulted only when no deny fired — the contract's first `warn` emitters: `heredoc-oversize` (a body of 4,096 bytes or more; a disclosed heuristic for the harness's dropped-heredoc class) and `msys-rev-path-subshell` (win32 + Bash: a `<rev>:<path>` token inside `$( … )` in a segment the `MSYS_NO_PATHCONV` assignment does not reach). Heredoc bodies are blanked before the command is tokenized, so a `--body "/x"` inside a heredoc is never read as an argument. Every verdict cites `provenance.source: 'transport-shield pattern table'`, `ref: <row id>` and a bounded, control-character-free `matched` fragment. A shape the table lacks is allowed: the gate enumerates known shapes, and a miss is a corpus gap, never a fail-open of an applicable gate.

**The registry carries each gate's matcher.** New core exports `knownGates()` (`[{ event, matcher }]` in registry order), `gateMatcher(event)` (throws `GATE_INVALID` on an unknown event, never a default), the `GateMatcher` and `GateDefinition` types, and the transport-shield module's `TRANSPORT_SHIELD_EVENT`, `TRANSPORT_PATTERNS`, `transportShieldEvaluator`, `parseTransportShieldPayload`, `HEREDOC_OVERSIZE_BYTES`, `MATCHED_FRAGMENT_MAX` and their types; `knownGateEvents()` now lists both gates. `installGates` takes `{ event, matcher }` pairs resolved from the registry (the `Write|Edit` constant is gone from the installer; `PreToolUseMatcher` gains `Bash|PowerShell`), so each gate installs under its own registry matcher; the upsert stays keyed on the gate's `--event` within that matcher, and an entry a hand edit left under a foreign matcher is left as-is (the upsert's standing contract). Deny rows are scoped to the program they name: every row reads the segment's PROGRAM position (after `VAR=VALUE` assignments and wrappers such as `env`, `sudo`, `time`, `xargs`), so `grep gh -b /x` or `git log --grep sed` is an argument, never an invocation; `msys-body-slash` reads `gh` only (`-b` means something else on `diff`, `curl`, `cp`, `sort`, `du`, `grep`) and honours `MSYS_NO_PATHCONV`, the cure it names, through the shell forms that export it to the judged program (MSYS reads its presence, any value): the segment's own `VAR=… prog` prefix, an earlier `export` / `declare -x` / `typeset -x`, a bare assignment under `set -a` or followed by `export NAME`, until an `unset` — never as a substring (a comment, a heredoc body or a quoted string naming the cure opts nothing out; an export inside a subshell or a pipeline element is taken as reaching later segments though the shell would not apply it, a disclosed over-allow); `sed-i-escape` reads the expression operand only, never a filename, a `-f` script path or BSD sed's empty `-i ''` suffix. Heredoc bodies never open a quote for the scanner (an apostrophe in prose does not hide a later heredoc), a terminator is an exact line as bash reads it, and a heredoc inside a `bash -c "…"` operand is a disclosed corpus gap. The scanners read the shell's grammar where a mis-read would desynchronize them: a `#` that begins a word — after an unquoted blank, newline, `;`, `|`, `&`, an opening `(` or an operator `)` — is a comment to the end of the line, discarded without quote processing (an apostrophe in a comment hides nothing; comment text is never an argument), while a `#` that continues a word is not one (`a#b`; `$(x)#a`, where the scanners know that `)` closes a substitution); `$(( … ))` / `(( … ))` arithmetic is skipped (a `<<` shift is not a heredoc); a backslash-quoted delimiter (`<<\EOF`) is a quoted heredoc and a delimiter word is read whole as bash delimits it (`EOF.TXT`, `1EOF`; a `$X` delimiter is read literally, as bash reads it — a delimiter word is never expanded); an unquoted backslash-newline is a continuation, never the newline `sed-i-escape` refuses; a background `&` ends a segment; `env VAR=x prog` reaches the program; every `--body` / `-b` occurrence is read, not the first; a `$(` inside single quotes is literal to the warn row; a `)` inside quotes never closes a `$( … )` or `<( … )`; and for the PowerShell tool the scanners themselves read PowerShell's grammar where it differs from bash's, in the same walk that tracks quotes and comments — one model, no pre-pass: a `<# … #>` block comment outside quotes is skipped whole, the backtick is the escape inside a double-quoted string, and a `#` begins a comment right after a string that began its token (`'a'#b`) or after the assignment operator (`$x=#c`), as PowerShell's own tokenizer reads them, while `x='a'#c` and `a=#b` stay single tokens (here-strings are not parsed — a disclosed gap). Per ADR-109 the gate, being the registry's first non-exact-match gate, ships a stated false-positive budget — zero denies over the benign corpus in `transport-shield.fold.test.ts` — and the fixture that measures it.

**The distributed PreToolUse wrapper projects a payload per event.** `.claude/hooks/gate-wrapper.cjs` keeps freeze-check's subsystem guardrail behaviourally unchanged (the same exits and messages; the lines now sit inside the event branch) and, under `--event transport-shield`, exits 0 as not-applicable unless the envelope's `tool_name` is `Bash` or `PowerShell` with a non-empty `tool_input.command`, in which case it shells `gate check` with `{ tool, command, platform: process.platform }`; a baked event it cannot project fails closed (exit 2). For every event the wrapper now hands the payload to `gate check` on the child's stdin (`--payload -`, a new form the verb accepts beside the JSON argument) — never argv, whose win32 limit of 32,767 characters a long Bash command exceeds, which failed the spawn and fell into the fail-closed arm with nothing broken; a wrapper installed before this cut keeps passing JSON on argv and still works. The reverse pairing — a wrapper written by this cut against a repo-local `@mmnto/cli` from before it — fails closed (exit 2) on every event, freeze-check included, because the old verb cannot parse `-`; that is the contract for an applicable gate that cannot be evaluated, and the cure is the matching CLI (`gate install` writes the wrapper from the CLI that will evaluate it, so the pairing arises only from a later downgrade of `node_modules`). Because the wrapper is marker-bounded, every `totem gate install` refreshes it in the same run that writes the entry — a consumer never holds the new entry with the old projection. `totem eject` now removes every INSTALLED gate hook whatever its matcher — the needle is the exact grammar the installer writes, `node <…/>.claude/hooks/gate-wrapper.cjs --event …` — and it removes the Totem HOOK, not the entry: a user command that shares an entry with a Totem hook survives, as does a user hook that mentions the wrapper's basename, runs a same-named wrapper from another directory, or passes `--event` to another program.

**Not in this cut:** a `gate-config` doctor detector (presence stays attested; a separate slice), gate re-installation from the prepare wrapper (gates remain opt-in repo policy through `gate install` / `init --gates=`), the Gemini BeforeTool template, and any change to the `allow | warn | deny` contract. The positive and negative corpora ride as tests, one assertion per row, and are recorded on the issue before any repo's hook set carries the gate.
