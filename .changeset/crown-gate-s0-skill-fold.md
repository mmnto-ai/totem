---
'@mmnto/cli': minor
---

feat(skills): crown-gate + S0 seat-identity fold into the distributed skill constants (mmnto-ai/totem-strategy#488 R-table; operator-ruled 2026-08-13/14)

The `DISTRIBUTED_CLAUDE_SKILLS` signoff and signon constants now carry the seat-scoping contract natively (previously strategy carried it as a repo-local after-marker extension):

- **Signoff step 0 — the crown gate, derived FIRST:** a repo with NO `totem:agent-bus` marker is UN-BUSSED — the gate does not apply and the full signoff runs (so non-cohort `totem init` consumers and un-bussed cohort repos see no behavior change; this matches the parity sensor's honest-absent semantics). Marker present: crown = the seat named `primary=`, derived fresh each signoff (the R7 ruling normalized every cohort marker to this one grammar; dual-grammar declined). Marker present with no `primary=` = crown VACANT — upkeep stages, mail the operator, no self-promotion. Non-crown signoffs are seat-anchored + own-lane: surface-upkeep mutations are crown-only, every other managed step still runs, read-only derivation stays open to every seat. The journal own-`owed:`-edges-only rule ships for every seat here (killing the first strategy#488 R9 residual), including the self-held→live asymmetry that produced the 2026-08-13 incident.
- **Signoff step 4 (the R8 named design choice):** stale-`[gone]`-branch cleanup is CROWN-scoped by name — it mutates the shared per-repo working tree, which is cohort upkeep, not own-lane; a non-crown seat reports `[gone]` branches to the crown instead. Named, not silent, per the ruling's one-wrong-option clause.
- **Signon step 2 — seat-anchored poll + the S0 identity check:** poll as your seat (per-shell `TOTEM_SELF_AGENT` or `--as <your-seat>`) and read the banner BEFORE any listing. The 1.117.0 identity gate is the deterministic floor for the ambiguity class; S0's residual is the wrong-single-identity class (a mis-scoped env naming a foreign seat resolves single-seat, ungated). Foreign seat or gated poll = stop-scope: read nothing, fix identity, re-poll.
- **Signon steps 1/3 seat-scoped** (the second strategy#488 R9 residual dies here): the journal consumed at signon is YOUR SEAT's latest — and step 1 now states what the SessionStart hook actually guarantees (it serves the seat it is CONFIGURED for, not a derived identity; a visiting session receives the HOST seat's journal), rather than asserting seat-correct injection no mechanism provides.

Four-skill audit re-verified with the fold: review-loop and review-reply carry no crown-duty or seat-agnostic-duty instructions (unchanged). Distribution: the constants reach `.claude/skills/` natively on `totem init` regeneration; the `.agents/` twins propagate via the doctor parity row going red — for repos that HAVE `.agents` twins (absence stays `skip`, so a consumer that never had twins gets no signal; the Claude-only reach gap remains open until mmnto-ai/totem#2532 slice 2 lands the committed home, exactly as the accepted plan states).
