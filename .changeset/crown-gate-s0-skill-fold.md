---
'@mmnto/cli': minor
---

feat(skills): crown-gate + S0 seat-identity fold into the distributed skill constants (mmnto-ai/totem-strategy#488 R-table; operator-ruled 2026-08-13/14)

The `DISTRIBUTED_CLAUDE_SKILLS` signoff and signon constants now carry the seat-scoping contract natively (previously strategy carried it as a repo-local after-marker extension):

- **Signoff step 0 — the crown gate, derived FIRST:** crown = the seat named `primary=` in the repo's `totem:agent-bus` AGENTS.md marker, derived fresh each signoff (the R7 ruling normalized every cohort marker to this one grammar; the dual-grammar reading was declined). No `primary=` = crown VACANT — upkeep stages, mail the operator, no self-promotion. Non-crown signoffs are seat-anchored + own-lane: surface-upkeep mutations are crown-only, read-only derivation stays open to every seat. The journal own-`owed:`-edges-only rule ships for every seat (a copied foreign carryforward line mints a live foreign edge in the copier's chain).
- **Signoff step 4 (the R8 named design choice):** stale-`[gone]`-branch cleanup is CROWN-scoped by name — it mutates the shared per-repo working tree, which is cohort upkeep, not own-lane; a non-crown seat reports `[gone]` branches to the crown instead. Named, not silent, per the ruling's one-wrong-option clause.
- **Signon step 2 — seat-anchored poll + the S0 identity check:** poll as your seat (per-shell `TOTEM_SELF_AGENT` or `--as <your-seat>`) and read the banner BEFORE any listing. The 1.117.0 identity gate is the deterministic floor for the ambiguity class; S0's residual is the wrong-single-identity class (a mis-scoped env naming a foreign seat resolves single-seat, ungated). Foreign seat or gated poll = stop-scope: read nothing, fix identity, re-poll.
- **Signon steps 1/3 seat-scoped** (the strategy#488 R9 residuals die here): the journal consumed at signon is YOUR SEAT's latest, never another seat's newer journal on a multi-seat repo.

Four-skill audit re-verified with the fold: review-loop and review-reply carry no crown-duty or seat-agnostic-duty instructions (unchanged). Distribution: the constants reach `.claude/skills/` natively on `totem init` regeneration; the `.agents/` twins propagate via the doctor parity row going red until mmnto-ai/totem#2532 slice 2 lands the committed home — the detector is the forcing function, as accepted in the fold plan.
