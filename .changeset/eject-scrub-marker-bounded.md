---
'@mmnto/cli': patch
---

fix(eject): the reflex scrub is marker-bounded and covers the full tool-table roster (mmnto-ai/totem#2602). `totem eject` now removes each complete `REFLEX_START..REFLEX_END` span with the same single-owner seam discipline regen uses — the span after an end marker is contractually user content and survives; the pre-fix heading-to-EOF regex deleted it and left an orphan start+version pair that read as `current`. Pairing is end-anchored (`lastIndexOf`), so orphan-marker damage from the pre-fix scrub sitting above a real block never widens the removal span, and the scrub loops until no complete pair remains, so double-injection damage clears. Unpaired marker residue is never scrubbed: those bytes stay and the summary says so — a residual version marker still reads as `current` to init, so healing is manual. The legacy no-marker path now bounds a v1 block at the next non-Totem H2 (mirroring init's own upgrade path) instead of EOF, and applies only to `CLAUDE.md`/`.cursorrules`, the files that predate the marker era — on the rest of the roster a bare heading can only be user-authored and is never touched. The roster itself is derived from the same `AI_TOOLS` table init injects through; the hardcoded pair silently skipped `GEMINI.md`, `.junie/guidelines.md`, and `.github/copilot-instructions.md`. Disclosures: seam whitespace normalizes exactly as regen would (a head-seam newline run collapses to one `\n`, so the scrub is a seam contract, not a byte-restore of the pre-init file), and per-file IO failures degrade to a reported skip instead of aborting the eject.
