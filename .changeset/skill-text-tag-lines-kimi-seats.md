---
'@mmnto/cli': patch
---

Two distributed skill-text fixes, re-stamped by `totem init`. The `review-reply` skill's step 2 binds the one-tag-per-line practice `doctrine/bot-protocols.md` § Tagging binds: each bot's @-tag on its OWN line, operator-ruled 2026-09-15, stated as practice and not as a GCA guarantee (position within the comment is unruled); the observed datum behind the ruling lives in the doctrine, not in the template, so the template adds no private reference a consumer cannot resolve. The `signoff` skill's step-2a cohort table fills the three Kimi cells that read "not seated" while `strategy-kimi`, `lc-kimi` and `status-kimi` were seated (mmnto-ai/totem#2865), and names the table for what it is: the bootstrap fallback — three of the roster's five vendor columns plus two archived repositories — that rots when a seat is added; `totem mail --derive-seat` is the derivation and no cell overrides it. A new test holds the table to core's `COHORT_AGENT_MAP` (every seat the map carries is in the table, and the table leads the map by exactly the four Kimi seats until mmnto-ai/totem#2875 propagates them, at which point the lock tightens to equality). (The packages share one fixed changeset group, so every `@mmnto/*` package takes the same patch bump.)
